<!--
	Never rendered: the server load for this route always redirects to `/`.
	The leaf component exists only so the route has a page node for both cold
	requests and client-side navigation.
-->
<p>Redirecting to <a href="/">Home</a>…</p>
