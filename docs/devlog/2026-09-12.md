# Devlog — 2026-09-12

## 00:13 EDT — Selective family-description quality refresh (td-e78d7b)

- Reviewed all 250 initially published family descriptions with Sonnet 5; separately inspected Crows and Jays when the normal worker completed it. All 251 families now have ready descriptions.
- Published 40 materially stronger accounts after source auditing, comparison with the old account, and editorial inspection. Preserved all 210 other existing publications. Hylocitrea remains thin; Przevalski’s Pinktail and Yellow Flycatchers did not benefit from rewriting their existing evidence.
- Added operator tooling for resumable review, source selection, targeted recovery and transactional compare-and-set publication with predecessor archival. Better species/genus accounts resolved initially disappointing family-page rewrites, including the Sylviid range ambiguity. Source bundles retain current taxonomy membership and explicitly distinguish selected examples.
- Wikidata Action API lookup verifies exact scientific name, rank and avian ancestry, follows pagination, and respects request pacing/rate limits. A source-audit clarification allows selective summaries without requiring every covered species; a deliberately false control remained rejected.
- Production content and sources match all approved artifacts, hashes match current taxonomy, all replaced versions are archived, no other publications changed, and every refreshed account retains the 180-day interval. Family pause is off and no cooldown remains.
- Validation: 26 focused tests, type/framework checks with zero errors/warnings, production build, and authenticated WebKit at 390/1200px for Wattlebirds, Asian Barbets (29 linked sources), and New World/African parrots. Production health is good.
- This was a data operation on app revision 1f0bb0f. Supporting code remains local and uncommitted; no application deployment was performed. Unrelated worktree files were preserved.
- Full outcome table and evidence: [quality review](../2026-09-11-family-description-quality-review.md). Private drafts, sources, publication manifests and verification are under .local/family-quality-*.

## 00:55 EDT — Personal Special interest collection (`td-e73377`)

- Owner authorized planning and implementation, incorporating `td-ce9a96`.
  Closed that older issue as a duplicate. Plan saved as matching Markdown/HTML:
  `docs/2026-09-12-special-interest-plan.{md,html}`.
- Added an explicit star/text species-page toggle and a Special interest Field
  Guide tab. The collection supports name/species-code/banding-code search,
  alphabetical/recent sorting, saved dates and removal, with no hidden row cap.
  Retired taxonomy codes remain visible as unavailable and can be removed.
- Added Special interest only to Browse species, preserving it across search,
  tag/family/geography filters, paging and species return links. SQL intersects
  personal selections before counting and pagination; unenriched saved species
  remain eligible. Saved badges accompany Seen/Need and Viewed badges.
- Personal reads/writes use the actual account, including viewers. Added only an
  exact POST exception to the viewer hook. Requests validate origin, body,
  account identity and current species. Explicit set/remove operations preserve
  saved dates on duplicate requests. Migration 0060 adds the per-account key,
  timestamp and account deletion cascade without a taxonomy foreign key.
- Save feedback waits for server confirmation. Lost-response retries repeat the
  intended selection. Keyboard removal returns focus to the collection count;
  WebKit form controls and buttons meet 48px targets. Four Field Guide tabs use
  two columns on phones and four on desktop. Help and About updated.
- Validation: 71 tests across 11 files passed, including real PostgreSQL account
  isolation, concurrent/repeated saves, preserved dates, account deletion,
  retired entries, 120 saved species across two guide pages, family/tag/region
  intersections, API/hook rejection cases and existing detail/history regressions.
  `npm run check`: zero errors/warnings. `npm run build`: app and worker passed.
  `git diff --check` passed.
- Authenticated desktop WebKit at 390/1200 pixels passed keyboard save,
  server-committed/lost-response retry, reload and second-session persistence,
  viewer isolation, collection search, filtered detail/back navigation, retired
  and current removal, restored focus, owner-write denial and stale-account
  rejection. No overflow or browser exceptions. Screenshots visually inspected.
  Initial harness runs needed to wait for SPA navigation/invalidation before
  asserting URLs or reloading. Visual QA corrected collection spacing, tab
  wrapping and native WebKit select sizing before the final pass.
- Evidence: `.local/special-interest-{tests,check,build,browser}.log`,
  `.local/verify-special-interest.mjs`, `.local/special-interest-{detail,guide,collection}-{390,1200}.png`.
  These are desktop WebKit tests, not physical iPhone verification.
- Applied migration only through the isolated birds_test migration runner.
  Test accounts were removed. No production operation, commit or deployment.
  Existing family-quality code and other unrelated worktree files preserved.
  Notes, multiple lists and automatic trip integration remain future scope.

### Special interest release authorized

Owner requested deployment. Release scope is Special interest (`td-e73377`),
including migration 0060. Existing family-quality changes remain outside this
release. Validate the clean release checkout, then deploy with the standard
health-gated script and verify the authenticated production flow.

### Release smoke correction — species response headers

The first production smoke found HTTP 502 on Osprey detail while the app logged
HTTP 200. Nginx logged `upstream sent too big header`. An authenticated request
directly to the app measured 4,088 bytes of header fields (plus HTTP status line
and terminator), including 3,825 bytes of SvelteKit asset preload links. This
exceeds the platform's 4KB initial proxy response buffer. Added Birds-scoped
8KB response-header/body buffers with a compatible 16KB busy-buffer setting;
no shared global Nginx settings change. Preserve preload links and page content.
Validate the candidate with production Nginx before the standard redeploy.
Reference: https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_buffer_size

### Ribbon queue and strongest-region readout — td-81a92d / td-7d86c7

- Created epic `td-81a92d` under original refinement parent `td-1775b4`, retaining
  the audit at baseline `95c3004` and this explicit execution order:
  `td-7d86c7` strongest region, `td-e7a96d` checklist counts, `td-b92412`
  relative colour, `td-4b5248` migration header estimate. These are work-order
  steps, not artificial dependencies. Centroid distortion remains deferred.
- Implemented the first item: the fourth readout line ranks fetched drill rows
  by the selected month's reporting rate with at least 40 checklists that
  month. Annual peak still determines the endpoint's maximum 40 candidates;
  the readout names that cap, and How-it-works/Help explain its limitation.
  Region-code ties are deterministic; a well-sampled zero stays eligible;
  no eligible rows gives the explicit none-with-40+-checklists message.
- The line requires a successful response matching species/band/continent,
  including the render before a new fetch effect runs. Loading/errors hide it.
  The aggregate cell readout is untouched. Its native 48px button reuses the
  drill selection/chart/scroll action and expands the list if the winner was
  outside its first eight rows. No API/schema change.
- Updated the mockup oracle, build spec rev 4, Help and About. Added five pure
  regression tests for month-vs-peak, 39/40 threshold, none, zero and stable
  ties/non-mutation. Six focused suites pass, 192 tests; check reports zero
  errors/warnings; production and worker builds pass.
- Desktop WebKit at 390x844 and 1200x844 against the existing isolated test
  server/DB verified actual Osprey selections (Jan: Coimbra 15%; Feb: Umbria
  19%; Sep: Ormož 44%), >=48px target, no horizontal overflow, keyboard chart
  selection and scrolling. Browser-only response overrides verified loading,
  error, none, zero and selection of the ninth row in a capped response. No
  browser exceptions. Mockup WebKit smoke also selects/charts its strongest
  region. Phone/desktop screenshots inspected. This is not physical iPhone
  verification. Initial harness assertions were corrected for responsive
  geography selection, subpixel scroll position and cap-qualified none text.
- Evidence: `.local/verify-ribbon-strongest.mjs`,
  `.local/ribbon-strongest-{tests,check,build,browser}.log`,
  `.local/ribbon-strongest-{390,1200}.png`. Temporary test account removed.
  Source changes remain uncommitted; no production operation or deployment.

### Strongest-region release authorized

Owner requested deployment of `td-7d86c7`. Reviewed the eight-file release diff;
192 focused tests, check (0 errors/warnings), build and WebKit results above
cover this exact code. Release via the standard deployment script, then verify
public revision/DB/worker health and the authenticated production readout and
chart action at phone and desktop widths. Deployment evidence is recorded on
`td-7d86c7` and the `td-81a92d` queue epic.
