# 2026-06-29 Devlog

## Birds app UX/data pass: timeframe, counts, return links, Google place IDs

### Summary
Implemented the Birds app mods plan and submitted `td-b57023` for review.
The optional Trips-DB historical-stop source was split into follow-up `td-4ce4cb`
as a P2 task.

### What Changed
- Near Me now accepts `?back=7|14|30`, shows a Window dropdown, and uses the
  selected window for nearby needs.
- Near Me and Targets species rows now show both eBird report counts and total
  birds reported. Expanded per-place rows also show report and bird totals.
- Species drilldown links now preserve origin with a safe local `returnTo` and
  pass the selected report window. Species pages render `Near Me` or `Targets`
  back links to the exact originating view.
- Added `backend/db/migrations/0007_google_place_ids.sql` with:
  - `users.home_google_place_id`
  - `trip_stops.google_place_id`
- Ported the Trips place-id model into Birds:
  - server geocode and reverse-geocode responses carry Google `place_id`
  - `MapPicker` preserves `place_id`
  - home location, custom trip stops, and generated historical stops persist
    Google place IDs when Google returned one
  - Google Maps links prefer `query_place_id` / `destination_place_id`, then
    fall back to exact coordinates only
- Settings now reports last synced/fetched dates and counts for taxonomy, life
  list, gallery links, eBird cache, trips, and stops.

### Verification
- `npm run check` — pass
- `npm test` — pass, 4 files / 10 tests
- `npm run build` — pass
- `git diff --check` — pass
- Browser QA on `http://127.0.0.1:5178/` against migrated `birds_test`:
  - Near Me Window changed to 14 days and URL became `/?back=14`
  - species links included `back` and `returnTo`
  - species pages rendered correct `Near Me` / `Targets` return links
  - Targets filtered route preserved `place`, `dist`, and `back` in return links
  - Settings showed sync/count status
  - mobile smoke at 390x844 passed

### Notes
- Standard `npm run migrate` against the default dev DB could not run because
  no local `.env` / `MIGRATION_PGPASSWORD` was present. Migration was applied to
  `.env.test` / `birds_test` for QA.
- Browser console showed only existing Google Maps marker deprecation warnings.
