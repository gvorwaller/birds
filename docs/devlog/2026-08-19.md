# 2026-08-19 Devlog

## 00:05 EST - Nearest lifers + weekly migration timing deployed

### Summary
Deployed both marquee features at 07dfc4c (prod healthy). First cycle
with the new AGY-advisory planning loop: AGY UX feedback -> GROK binding
pins -> build -> dual review. Also survived and diagnosed a relay
delivery drop (CODEX1's verdict was sent + marked delivered but never
reached CC1; recovered from the wake-job journal on disk).

### What Was Done
- /nearest ("Nearest lifers", drawer entry): auto-runs the top 6
  likely-band month targets from the saved home via eBird's previously
  unused nearest-observations endpoint (unbounded distance, provisional
  included, 30-min cache); needs-only search; two-line distance-hero
  rows; every GROK empty state.
- Species pages: on-demand "Check nearest reports" card (need species
  only, query-preserving CTA, saved-home gate).
- FrequencyChart Month|Week toggle: all 48 stored weeks drawn (AGY's
  320px math), hatched low-sample bars, nodata dots, effort sparkline
  on /forecast/species; "arrives ~X . departs ~Y" sentences emitted
  ONLY for supported migratory shapes (adequacy-gated presence/absence,
  exactly-one absence season; double-gap passage suppressed).

### Key Technical Insights
- Circular-run scanning: modulo in the run-extension loop double-counts
  the wrapping run (re-walks head indices) — scan linearly, then merge
  the boundary pair.
- hydrateEbirdLocationPlaceIds defaults to LIVE Google Places
  resolution for unknown locs — deadly on unbounded surfaces (CODEX1
  P1: 30 hidden calls/view); pass resolveMissing:false.
- Relay delivery can drop replies while marking them delivered (Codex
  app now investigating as designated relay debugger); wake-job
  journals at ~/claude-relay/data/jobs preserve verdict summaries.

### Next Steps
- CODEX1 to approve td-a6c322 + td-af8393.
- Remaining P2s: life-list map + timeline (the big one), taxonomic
  sort/banding codes, trip forecast-vs-actual, photo flags, tides.

## 17:30 EST - Enrichment UX built: instant refresh, honest labels, no_sitelink fallback (td-b7d021)

### Summary
Implemented the full td-b7d021 cycle in three commits (04ebf77, d55c3e5,
7cf2f1f): the parked daily scan no longer renders as queued work, the
manual species refresh is now inline (~1s live) with honest copy, and
no-sitelink taxa (whimbr3) get their article via the binomial redirect.
CODEX1 caught one real blocker (partial write on transient), remediated
same session; awaiting final CODEX1 + GROK verdicts.

### What Was Done
- **Commit A (04ebf77)** — `isScheduledSingleton` (recurring singleton +
  pending + future next_retry_at + not waiting_retry) → API `scheduled`
  flag → chip/poll/recent all skip it; hub SSR "Next enrichment scan —
  tomorrow 1:11 PM ET" line; `enrich_species` type name → "Species
  data"; refresh label = com_name.
- **Commit B (d55c3e5)** — `wikiFetchTitleFor` as the ONE sitelink-else-
  binomial decision (action AND worker); `enrichOneNow` wiki-only inline
  refresh under a 20s AbortSignal, AI never inline (aiOnly chunk
  enqueued on ok+aiDue); GROK's exact copy set; worker fallback with
  MISS keeping no_sitelink + restamped clock; skip-probe rescue for
  no_sitelink on retry_due only; weekly lane extended MISS-only; About
  note on the fallback path; Help updated in-commit.
- **Remediation (7cf2f1f)** — CODEX1 blocker: upsertResolution ran
  before the article fetch, so a Wikipedia 503 left a partial write
  despite the zero-write pin. enrichOneNow now completes ALL network
  fetches before any persistence; DB failures throw. Also his DST
  follow-up: hub "tomorrow" now derives the next calendar date in
  app TZ (src/lib/next-scan.ts), not now+24h.

### Files Modified
job-policy.ts, api/jobs/+server.ts, job-poll-core.ts,
job-poll.svelte.ts, species-enrichment.ts (+enrichOneNow, lane,
wikiFetchTitleFor), job-handlers.ts (worker fallback, probe rescue,
unconditional taxa), species/[code]/+page.server.ts (inline action),
species/[code]/+page.svelte (About note, Refreshing…),
forecast/data/+page.server.ts/+page.svelte (nextscan line),
next-scan.ts (new), help/+page.svelte, tests (+13 total).

### Key Technical Insight
- The "5-minute enrichment" was never slow jobs: the parked daily scan
  singleton rendered as "Load queued" all day AND held the 2.5s poll.
- enwiki redirects=1 + storing the RESOLVED page.title makes the
  binomial fallback safe without a mismatch detector (GROK veto held).
- Inline actions that fetch-then-write must sequence ALL network before
  ANY persistence, or "transient" becomes a lie (CODEX1 blocker).
- Full-suite runs can flake if the dev server's worker is draining jobs
  against the same test cluster (5 transient failures, reran clean).

### Next Steps
- Await CODEX1 re-review + GROK verdict on 4f1a1ea..7cf2f1f; fold in
  AGY advisory if it ever arrives (still offline).
- Hold for Gaylon's deploy word; then prior picks: life-list map +
  timeline (td-b5986c), taxonomic sort (td-5bc1d4), tides (td-6a3d2e).

## 17:45 EST - td-b7d021 round 3: atomic persistence + GROK P2s (56dc274)

### Summary
GROK APPROVED the range with 2 P2s; CODEX1 rejected a second time on a
real remaining blocker (persistence phase was two autocommit writes, not
a transaction). All fixed in 56dc274; both reviewers re-pinged.

### What Was Done
- Enrichment writers take an injectable executor; enrichOneNow commits
  resolution + wiki outcome + aiDue read through ONE withTransaction
  client. Fault-injection test via real SQL: BIGINT-overflow revid fails
  the wiki write in-txn → whole phase rolls back byte-identically.
- GROK P2-1: happy path returns queued {jobId, label "Field craft"} so
  the idle poller's chip tracks the background AI stage (live-verified).
- GROK P2-2: handler tests pin the anti-loop — fresh no_sitelink + WDQS
  hit never opens Wikipedia; weekly retry_due runs the binomial fallback.
- GROK nits: FORECAST_CALENDAR_TZ import in next-scan.ts; hub SSR line
  excludes waiting_retry; AI_STAGE_ENABLED moved to job-policy.

### Key Technical Insight
Fault-injection through real SQL (an overflowing BIGINT parameter) tests
transaction rollback without mocking the DB layer — the failure happens
exactly between the two writes the transaction must bind together.

### Next Steps
- Await CODEX1 verdict on 56dc274 + GROK P2-slice re-check.
- Hold range 4f1a1ea..56dc274 for Gaylon's deploy word.
