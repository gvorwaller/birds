# 2026-09-05 — Migration ribbon mobile cell selection & UX

## iPhone heatmap cell selection & intuitive UX (`td-2c7a0b`)

- **iPhone Heatmap Cell Selection (AC 1–6):**
  - Mapped pointer client coordinates through the SVG viewBox scale ratio in `onPointerUp`: `(e.clientX - r.left) * (geom.w / r.width)` and `(e.clientY - r.top) * (geom.h / r.height)` to prevent subpixel scaling drift across mobile Retina screens.
  - Enabled whole-cell selection on mobile taps (`bandOnly: false`), allowing iPhone Safari taps anywhere inside a heatmap block to select that exact latitude-band and month cell.
  - Preserved the `>= 8px` pointer travel threshold so vertical scrolling never triggers accidental cell selection.
  - Retained $\ge 48\text{px}$ touch target row heights below 640px per `cs.md`.
- **Empirical Seasonal Range Cards:**
  - Added structured summary cards to the migration ribbon: `Northern Range`, `Southern Range`, and `Transition Months` for migratory shifts ($\ge 8^\circ$ centroid delta), and `Year-Round Range` / `Observed Range` for stationary distributions.
  - Formulated strictly from empirical eBird checklist reporting frequencies without ungrounded biological breeding assumptions or hemisphere-biased directionality.
- **Species Primary Column Defaulting & Contract Alignment:**
  - Initialized desktop All-continents view to start on the species' data-derived primary column (`primaryContinent(grid)`) so species outside the home column (e.g. European Robin) immediately display occupied bands rather than blank space.
  - Phone strictly opens in World view (`view: 'world'`), preserving the spec contract.
  - Updated release documentation in About (`src/routes/about/+page.svelte`) and pinned with contract test in `about-route.test.ts`.
- **Adversarial Multi-Model Peer Reviews:**
  - **GROK:** Conducted review cycles via `claude-relay`. Following copy and contract synchronization, GROK independently drove Playwright WebKit at 336px, 390px, and 430px (2x/3x DPR) across Jan–Dec and issued `APPROVE` (`ses_631f39`).
  - **CODEX13 (Sol):** Conducted re-reviews via `claude-relay`. Addressed biological overclaiming and directional calendar assumptions by adopting neutral `Transition Months` and adding cross-equatorial inverse-season regressions. CODEX13 issued `APPROVE` on commit `b995da1`.
- **Verification:**
  - 138 focused unit and contract tests pass across `src/lib/components/migration-ribbon*`, `src/routes/about`, and `src/routes/help`.
  - `svelte-check`: 0 errors, 0 warnings.
  - Production build succeeds cleanly in ~3.2s.
