# 2026-09-10

## Field Guide study lists (`td-81600d`)

- Added shared **Browse species / Viewed species** tab navigation to Field
  Guide and `/viewed`. Existing links remain available. Both sections highlight
  Field Guide in the primary navigation; species detail pages retain their
  existing navigation behavior.
- Added **Study list: Viewed species / Not yet viewed** and **Group by: No
  grouping / Bird family / Country**. The study interpretation is absence of a
  recorded app-page visit, independently of life-list Seen/Need. Clarification
  questions were offered; with no reply, proceeded with that stated
  interpretation and bird-family/country grouping. State/region subdivisions
  are not included in this change.
- Families use the existing taxonomy family names, ordered alphabetically,
  with an explicit Family unavailable group if needed. Species retain recent
  or alphabetical ordering within groups; Not yet viewed uses alphabetical
  sorting. Retired codes remain available in viewed history.
- Country groups open independently and load only on expansion. Membership
  comes from recorded region/county/hotspot distribution sources; weekly rows
  deduplicate within a country, while the same species may appear in multiple
  countries. Loaded date ranges and coverage are explicit. No loaded data is
  distinct from no matching reports; species without mapped reports remain
  available in the other groupings. This is reported historical presence, not
  a range map or a claim of current/typical abundance.
- Country summaries query only coverage metadata. On the dedicated test
  snapshot (about 37.9 million weekly frequency rows), a distinct membership
  query across 8,023 US sources returned 1,235 species in about 1.8 seconds.
  Country reads have a 15-second DB query deadline and a visible Retry state;
  the browser request also has a deadline and is cancelled on navigation.
  No provider traffic or new schema migration is required.
- All study-list records remain accessible. Rendering uses explicit batches
  of 100 with a visible total and Show 100 more; collapsed families do not
  render their rows. Species return links preserve filters, the opened group,
  and enough displayed rows to include the selected species. Group anchors
  restore the relevant part of the page.
- Reads use the signed-in account, never the shared scope owner. The country
  endpoint rejects stale-account requests, validates countries, and sends
  private/no-store responses. Existing pause, resume, clear, first/last dates,
  badges, and recording behavior remain available. Updated Help and About.
- Verification: 60 tests across 12 focused files passed, including real DB
  complement/account/life-list separation, complete taxonomy access, literal
  search, multi-country membership, weekly deduplication, API guards, and the
  existing viewing-history regressions. Svelte check: zero errors/warnings.
  Production build and diff check passed. Authenticated desktop WebKit at
  390/1200px verified tabs, family collapse, owner/life-list independence,
  removal from Not yet viewed after a visit, Show more, lazy country loading,
  retry, repeated species across countries, and group/anchor return links.
  Screenshots were inspected; no horizontal overflow or browser exceptions.
  Physical iPhone and production behavior have not been tested for this change.
- Local evidence: `.local/verify-species-study.mjs`, `.local/study-browser.log`,
  `.local/study-{family,country}-{390,1200}.png`, and `.local/study-*.log`.
  Owner subsequently authorized commit and deployment through the standard
  release script. Final revision, health, and authenticated production checks
  are recorded on the td. The unrelated deferred longitude plan remains
  untouched.

## Study country filtering and counts (`td-81600d` follow-up)

- Owner requested that country groups behave like families: show only countries
  containing birds in the selected study list, with species counts before
  expansion. The loader now passes the exact account/list/search result codes
  to the country-count query. Empty countries are omitted; no mapped reports
  produces an explicit empty state without hiding birds from the other views.
- Counts deduplicate weeks and reporting locations, while a species may still
  appear in several countries. Geographic mapping uses the same validated
  region/hotspot country mapping as the coverage summary. Species rows still
  load on expansion. Help and Version History describe the new behavior.
- A small three-species count query on the isolated production-shaped test
  snapshot took 26ms in the initial benchmark; the full 11,167-species query
  took about four seconds. Large counts stream after the page controls render,
  with a 30-second query deadline and an explicit Retry state on failure.
- Validation: 29 focused tests passed, covering distinct counts across weeks
  and locations, different counts per country, empty/missing geography,
  viewed/unviewed account and search scoping, streamed-loader failures, and
  existing study/history behavior. Svelte check: zero errors/warnings;
  production build and diff check passed. WebKit checks verified collapsed
  counts, filtered countries, the no-mapped-reports state, changed search,
  full unviewed counting, lazy species loading/retry, and mobile/desktop
  layouts. A Mountain Bluebird search showed four matching countries, each
  with one species. No browser exceptions; screenshots inspected.
- Evidence: `.local/verify-study-country-counts.mjs`,
  `.local/study-country-count-{tests,check,build,browser}.log`, and
  `.local/study-counts-matching.png`. Owner subsequently authorized this
  follow-up release through the standard deployment script; no new migration.
  Final revision, health, and production verification are recorded on the td.

### Production count-query correction during release

- The first live smoke verified small viewed/search counts but found that
  full Not yet viewed counting exceeded its query deadline. Production plans
  overestimated the geography join and used expensive sorts of intermediate
  species/location rows; direct global grouping also remained too slow.
- Large study lists now reuse the bounded per-country membership query with
  two concurrent reads, then intersect each country's species with the exact
  selected study codes. Small lists retain the species-index query and join
  the real frequency-fetch primary key. Both strategies deduplicate species,
  omit empty countries, and preserve all records. The strategy threshold is
  an execution choice, not a data cap.
- A read-only production benchmark counted all countries in 23.1 seconds
  (slowest individual country: US, 10.4 seconds), within the individual
  15-second read deadline. Counts stream while page controls remain available.
  Added a real-DB parity regression for the large-list strategy; rerun checks
  and final production browser verification are recorded on the td.
