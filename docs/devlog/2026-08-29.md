# 2026-08-29 — Similar species conversion complete (td-460b1c)

The eBird-slash → iNaturalist misidentification conversion shipped end to end
and the Sonnet annotation wave finished overnight. The bug that started it
(Great Black-backed Gull showing no similar species) is fixed in production
with real prose: LBBG rank 1, the Herring complex behind it, reciprocal rows
from Caspian/Pacific Gull.

## What shipped (this session, three deploys)

- **Phase A** (911f059 + 19375e8): migration 0037 (`species_inat_similar` +
  `inat_*` stage columns), iNat gateway with strict envelope parsing,
  `enrich_species_inat` worker stage, round-robin lane quotas, page-load
  1-code enqueues. Backfill: 3,551 species → 46,426 confusion edges, zero
  `no_mapping`. Mid-run incident: iNat's REAL rate ceiling is far below the
  documented 60/min (~140 requests then ~30 min of hard 429s) — fixed with 3s
  per-request pacing + dropping the per-species name lookup, plus an ops
  reset (attempts=0, max_attempts=60) so chunks couldn't exhaust.
- **Phase B** (44c1ca5 + c3130d5 + 149d7ed): migration 0038
  (`species_similar_display`), reconcile engine (two-arm resolution, selection
  floors, reverse-current-source reciprocity, fingerprint short-circuit),
  note preservation via owed-only prompting, null-decline channel, stage-aware
  error handling, single-tier card with visible no_mapping/error states.
  CODEX1 ran a 3-round fix-it-yourself review loop (7 P1 + 4 P2, all fixed);
  its sandbox couldn't commit or run DB-gated tests, so this session verified
  (999 tests) and committed. Pre-flight gate (real builder, full corpus)
  measured the wave and caught the uncapped reverse union (hub species at
  42/64/100 offered) → reverse extras capped at 7 (Gaylon decision).

## The wave (final receipt)

- 2,835 metered Sonnet 5 calls; 9.30M input + 1.94M output tokens ≈ **$38.03**
  at intro pricing (vs Gaylon's $60 cringe estimate; pre-flight predicted
  2,748 calls + retries).
- 13,422 notes across 3,041 species (11,776 new Sonnet, 1,646 preserved Opus).
- 260 pairs terminally declined as not-genuinely-confusable.
- Final statuses: 3,005 ok / 508 none / 37 error (partial-miss shape — one
  malformed note among several; 7-day lane self-heals).
- Corpus: 3,049 species with reconciled display sets.

## Process notes

- Four adversarial review passes on the plan (CODEX1, GROK, AGY, self-review
  /code-review) — 30 findings, all resolutions logged in
  docs/2026-08-27-similar-species-inat-plan.md. GROK's no-ack relay protocol
  (first reply = finished findings) cured its stalling.
- Local birds_test is now a prod restore (memory: never size designs from
  local data — the corpus was 3,551, not the stale 1,219). Post-wave prod
  snapshot taken (data/backup/prod, 160MB).
- Model: enrichment stays Sonnet 5 by Gaylon's live-quality verdict
  ("Sonnet is doing great") — memory updated, do not steer back to Opus.

## Open threads

- td-8cecc6 (P3): souboo8 422s forever — Wikidata P3151 is garbage
  (322568937; correct id 979816). Durable fix is upstream on Wikidata.
- td-c41126 (P2): 3-4 DB tests broken by the prod-scale test bed.
- 37 error-lane species self-retry on the 7-day clock (or a manual aiOnly
  nudge).
- td-460b1c ready for independent review (CODEX/GROK per repo gate).
