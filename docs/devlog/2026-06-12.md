# 2026-06-12 Devlog

## 16:30 EST - Phase 1 MVP implemented (scaffold → working app)

### Summary
Built the entire Phase 1 foundation in one pass: SvelteKit app (madonnahist patterns), dedicated Postgres on 5436 (test cluster 15436), auth/sessions, encrypted eBird credential storage, credentialed life-list sync (CAS login), needs/targets explorer with rare-this-week, link-out gallery sync from gaylon.photos, and deploy artifacts. All routes verified live against the isolated test DB.

### What Was Done
- Scaffold: package.json (no Sharp/AWS — link-out gallery), svelte.config, vite (port 5178), tsconfig, app.html/app.css (mobile-first tokens from mockups)
- DB: migrations 0001 schema (users, sessions, user_ebird, seen_species, taxonomy_cache, species_match_overrides, photo_links, ebird_cache, trips/trip_stops), 0002 grants, 0003 seed admin; migrate_pg.sh + full test-isolation scripts (BIRDS_ENV=test, 15436, guards refuse 5433/5434/5435)
- Server lib: db.ts (query/withTransaction/health), auth.ts (argon2id), session.ts (30-day sliding, birds_session), crypto.ts (AES-256-GCM via EBIRD_KEY_SECRET), ebird.ts (typed client + TTL cache w/ stale fallback), ebird-account.ts (CAS login → lifelist CSV → import; fails soft), species-match.ts (override → common → scientific), gallery.ts (sync + rematch + health), needs.ts (region targets, notable badged seen/need, nearby needs w/ haversine)
- Routes: / (Near Me), /targets (region + window + rare-this-week), /photos (species groups, unmatched override workflow), /species/[code], /settings (all credentials + syncs + CSV fallback), /trips (Phase 2 stub), /login, /api/health ({db, gallery_source, version})
- Deploy: ecosystem.config.cjs (port 3003, 600M cap), deploy/nginx.conf, scripts/deploy-to-DO.sh (health-gated)
- Verified: migrations apply clean; npm run check 0/0; build clean; curl smoke test — health db=ok, login 200, all pages 200; first gallery sync pulled all 49 real photos (unmatched until taxonomy syncs, then auto-rematch)

### Files Modified
Everything under src/, backend/, scripts/, deploy/, plus package.json and env templates. ~45 new files.

### Key Technical Insight
- Port change: birds gets its OWN Postgres cluster on 5436 (5434 belongs to madonnahist) — cs.md, guards, env templates all updated.
- eBird API has no personal-data endpoints; life list rides the CAS web login (secure.birdcount.org) with a manual-redirect cookie jar. Every failure path records life_list_status='error' and keeps the last synced list.
- species_match_overrides FK to taxonomy_cache is DEFERRABLE INITIALLY DEFERRED so taxonomy re-sync (delete+reinsert in one tx) doesn't trip it.

### Next Steps
- User: set real password (set-password.ts), save eBird API key, sync taxonomy, save eBird login, sync life list — then targets go live
- Droplet provisioning: Postgres 5436 cluster + roles, /opt/birds, .env, DNS for birds.gaylon.photos, first deploy
- Phase 2: trips UI, Google Map on dashboard/targets, hotspot ranking

## 21:00 EST - Fix credential visibility, eBird CAS login host, map-based home picker

### Summary
Addressed three user-reported issues from first real use: (1) credentials saved but invisible, (2) eBird sync failed with "fetch failed", (3) lat/lon home entry rejected — wanted a map.

### What Was Done
- **Root cause of "fetch failed":** the CAS host `secure.birdcount.org` no longer resolves (NXDOMAIN). Real flow is `secure.birds.cornell.edu/cassso/login` (Spring Security form: `execution` + `_eventId=submit` + XSRF cookie, not the old `lt`). Rewrote casLogin accordingly; added testEbirdLogin(); verified end-to-end — "Test login" returns success with real account (gvsurf).
- **Credential visibility:** Settings now shows SAVED/missing badges, Reveal key, Remove key, "Saved account: <username>" (decrypted username shown; password never revealed), Remove login, and a Test login button.
- **Map-based home picker:** new `$components/MapPicker.svelte` (Google Maps + search box + tap-to-place + draggable pin), server `/api/geocode` (forward + reverse via GOOGLE_GEOCODING_KEY from gaylonphotos/.env), migration 0004 adds `users.home_label`. Replaced lat/lon inputs entirely. Verified: searched a park → marker dropped → saved with full address label.
- Added `http://127.0.0.1:5178/*` to the Maps key referrer allowlist (GCP console).

### Files Modified
backend/db/migrations/0004_home_label.sql, src/lib/google-maps.ts, src/lib/components/MapPicker.svelte, src/routes/api/geocode/+server.ts, src/lib/server/ebird-account.ts, src/routes/settings/+page.{server.ts,svelte}, cs.md, env templates

### Key Technical Insight
Vite auto-restarts the dev server when .env files change, so adding GOOGLE_GEOCODING_KEY to .env.test took effect without a manual restart. The eBird life-list CSV pull (Sync) is still unverified end-to-end — login half is proven; the CSV URL/format needs one real Sync run to confirm.
