# 2026-09-03 — Migration ribbon: TD-A + TD-B deployed, TD-C in gate

## Deployed (owner's word after his nap)
- **76b81a4** on the droplet at 23:27 UTC: migration `0050_species_band_rollup.sql`
  (band_locs / band_month_samples / species_band_month_freq, maintained per country by
  `rebuildBandRollup` inside `storeFrequencies`), `speciesRibbon()` + `ribbonRegions()`,
  the species-page loader's `ribbon:{ok,grid|error}`, and `/api/species-ribbon-regions`.
  No UI yet.
- **How:** local main also carried the four ungated TD-C commits and
  `scripts/deploy-to-DO.sh` ships origin/main whole, so `76b81a4:main` was pushed alone
  and the script's remote block was run by hand over SSH, line for line (pull, npm ci,
  build, migrate, pm2 reload, health gate). Gaylon declined moving local main.
- **Runbook evidence (td-8d3526):** pause requested 23:17 while job 3342 (Cambodia)
  finished; worker paused, no job, at 23:26. 0050 applied once at 23:27:36. Resume gate:
  new worker pid at 76b81a4, `state=paused`, `current_job_id=null`,
  `pause_requested=true`. Reconcile: contributor CTE vs `band_locs` both ways with
  EXCEPT → 959 = 959, 0/0; 20 sampled countries → 516 sample cells 0/0, 219,023 freq
  rows 0/0 on `num` and `reached`. Resumed 23:29. First job under the new worker
  (Cameroon, 10 regions) reconciled 0/0 live, 5,619 freq rows. Health
  `db=ok worker=ok version=76b81a4`. Totals: 959 contributors, 59 countries,
  502,077 rollup rows.
- The pre-deploy estimate said 948 contributors; the Cambodia load completed in between.
  The reconcile uses the migration's own rule inside one statement, so 959 is the number.

## Gates today
- **TD-B (td-c6b113):** CODEX1 REJECT at 178f437 (all-thin cell fell through to null,
  `Number('')` passing the band check, byte test throwing on a restored cluster, an
  over-broad snapshot claim, two untested paths) → fix 76b81a4 → CODEX1 ACCEPT, GROK
  ACCEPT (empirical: interleaved a committed rebuild between the three SELECTs and the
  snapshot held). Production payload for Rock Pigeon measured from the source tables:
  455 of 1,728 slots; GROK's varied-cell full-dense ceiling 21.5 KB gzip vs the 40 KB gate.
- **TD-C (td-950907):** built at 5074104; CC1 post-review sent back two P2s (fixture
  scripts destructive on a prod-restored test cluster; three new check warnings) →
  5fbc0d7 → CODEX1 REJECT 2 P1 / 3 P2 and GROK REJECT with the identical set →
  2d70a0c → CODEX1 REJECT 2 P2 (unseed check-then-delete with no lock; phone hint copy)
  and GROK PASS → bbc9426 → CODEX1 PASS; GROK re-check pending.
- **The oracle was wrong twice.** The mockup's own `pick()` set band AND month on phones,
  so the owner's P1-7 rule (rows own the band, the scrubber owns the month, 48 px rows)
  only ever lived in the spec; and its readout printed the small-sample line before the
  zero line. Both corrected in the mockup and verified live in Safari (16ca96a,
  999f31d). Spec now rev 3.5.

## Incident (CC1)
While exercising the new fixture scripts CC1 ran `unseed:ribbon-fixture` on the shared
test cluster and wiped the fixture GROK had seeded for its re-check. GROK was told and
re-seeded; nothing else was affected. Rule saved to memory: never touch birds_test
while a reviewer or implementer may hold a fixture.

## Open
- GROK's re-check of bbc9426 (unseed lock, hint copy, About).
- CC1 Safari drive at 336/390/1300 — blocked on an admin sign-in on the local QA window
  (CC1 does not type passwords, including the test one).
- Then TD-C to Gaylon for approval, and a TD-C deploy that pushes the rest of main.

## Later that night — TD-C gated (658efc7)
- Fix rounds after the deploy: 2d70a0c (phone band-only picking with 48 px rows in every
  phone view, drill generation bumped before the cache lookup, readout branching on the
  server's `low`, drill feedback reset, fixture marker bound to the DB), bbc9426 (unseed
  takes SHARE ROW EXCLUSIVE locks before its check-then-delete; phone tap hint), 40d0eb4
  (phone readout sticks to the viewport bottom — CC1's Safari drive found it ~800 px
  below the viewport after a tap on an upper row once rows were 48 px), 658efc7 (the
  sticky offset named `--nav-h`, the top nav, instead of `--bottomnav-h`; CC1 wrote that
  rule into the oracle and the implementer copied it — CODEX1 caught it; pins tightened).
- Gates at 658efc7: CODEX1 PASS, GROK APPROVE with Chrome safe-area emulation
  (bottom inset 34 px: readout fully in view above the nav, 1 px on the nav's border line
  and nothing hidden — ruled a non-issue). CC1 Safari drive at 336/390/639/640/700/1023/
  1024/1040/1300 passed; the one thing automation cannot produce, a trusted tap, Gaylon did
  by hand at 390 (screenshot on td-950907). Note for future drives: this Safari has classic
  scrollbars, so a 1024 CSS breakpoint trips at a 1041 px window.
- Local sign-in for QA without typing a password: a `qa-admin` user (argon2 hash,
  credentials in `.local/qa-admin-credentials.txt`, mode 600) and a session row whose id
  equals whatever `birds_session` cookie Safari already sends — Safari kept a stale
  HttpOnly cookie that page scripts could neither read nor replace, so registering its
  value as a session was the clean way in.
- Open: Gaylon's approval of td-950907 and the deploy word. TD-C carries no migration, so
  the deploy is the normal `scripts/deploy-to-DO.sh` run, which pushes the rest of main.

## 2026-09-04 — TD-C deployed (0be79f4)
- Gaylon approved td-950907 and gave the word; `scripts/deploy-to-DO.sh` pushed main and
  deployed: 0 migrations applied (49 skipped), pm2 reload, health `db=ok worker=ok
  version=0be79f4`. The migration ribbon is live on every species page. Parent td-59c2d0
  submitted for review.
