# 2026-08-17 Devlog

## 15:20 EST - Species enrichment: plan, mockups (AGY), Phase 1 dual-accepted

### Summary
Full arc in one day: researched per-species enrichment sources (eBird's
species prose is Birds of the World — paid, closed; Wikipedia/Wikidata via
P3444 is the licensed path), wrote the plan, dual plan-review (CODEX1 12
findings, GROK UX contract), AGY (Gemini, new relay peer) generated the
Phase-0 mockups Gaylon approved in Safari, then built Phase 1 and took it
through 3 CODEX1 rounds + 2 GROK rounds to dual-ACCEPT (70f5034 + 7ab7f95).

### What Was Done
- Migration 0020 species_enrichment (stage-separated resolution/wiki/ai
  state, wiki_ok_at attribution clock, writer-computed weighted tsv).
- wikidata.ts (batched SPARQL, BestRank quantities psn-normalized,
  deterministic lowest-QID duplicate resolution, Retry-After), wikipedia.ts
  (one extracts+revisions call, keyword section whitelist), gateway with
  scope/stale contract + AI phase gating.
- Jobs: enrich_species ≤30-code cancellable chunks (10-min wall budget,
  content-hash dedup, spillover), scan_enrichment recurring singleton
  (≤8 chunks/pass, 15min/24h adaptive successor), ensure at startup+idle.
- Species page About card: clamped lead + measured Read more/Show less,
  default-closed section disclosures with parsed subheads (wiki-render.ts),
  per-status AAA IUCN chips, facts badges with junk-claim spread guard,
  CC BY-SA one-line footer + revision-permalink disclosure, degraded
  states, admin-gated refresh.
- Review rounds fixed real bugs: scanner would re-enqueue no_mapping rows
  every 15 min forever; mixed transient failures terminalized green;
  obsolete prose outliving ok→no_article; failed refresh re-dating
  attribution; EN/VU IUCN chips rendering seen-green at AA contrast;
  raw === markers leaking into cards; tsv computed in a CTE that reads
  the pre-write snapshot (caught by own DB test).
- Live E2E: worker enriched the test cluster's real 815-species scope with
  ZERO unit failures; multiple Safari passes (both CC1 and GROK drove it).

### Key Technical Insight
Data-modifying CTEs are invisible to their outer statement (same snapshot)
— never "upsert then recompute" in one WITH; compute derived columns
inside the writing statement. And relay delegates wake only on DIRECT
messages — broadcasts deliver but wake nobody.

### Next Steps
Gaylon's deploy word for Phase 1 (migration 0020 via deploy script; prod
cold-run enriches his scope over ~2h of interleaved chunks). Then Phase 2
(AI tags + field craft — the td-47d6d5 tide answer) and Phase 3 (Field
guide search page).
