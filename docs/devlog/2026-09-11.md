# September 11, 2026

## Field Guide taxonomy (`td-c60291`, `td-5bc1d4`)

- Implemented the saved Markdown/HTML taxonomy plan. Added a Taxonomy tab with
  an educational introduction, order/family/species browsing, counts, search,
  explicit missing-classification states, and a retry page. Family descriptions
  are separately maintained, sourced editorial notes. Initial coverage is one
  family (Osprey); the UI reports that coverage and labels missing descriptions.
- Species family and order links open the taxonomy reference; species links
  preserve return context and highlight the selected bird. Taxonomy rows use
  the signed-in account's Viewed badges. Reading an order/family records no
  species visit and does not inherit a shared life-list owner's history.
- Migration 0057 adds nullable source ordering, order, family identifiers and
  scientific names, banding codes, reporting parent, and extinction status.
  Validated the complete real eBird response: 17,891 taxa, 11,167 species, all
  species classified; 2,253 species have banding codes. The validator explicitly
  accepts the genuine hyphenated spuh code `bird-o1`.
- Sync validates before replacing the cache and preserves atomic rollback and
  deferred override constraints. Worker results include metadata coverage.
  Applied the migration through the standard runner only on isolated
  `birds_test`; backfilled its metadata from the real public taxonomy response.
- Added family filtering to Browse species and taxonomic sorting to Browse and
  Viewed species, including country expansion. Existing defaults remain intact.
  Exact banding-code search supports case-insensitive and ambiguous matches.
  Browse species now exposes totals and pages of 100 with all filters and sort
  retained; no hidden 50-result truncation remains.
- Help and About updated. New database coverage includes malformed source
  rejection, transactional replacement, deferred-FK rollback, complete real
  family pagination, ambiguity, geography/family intersections, shared-viewer
  isolation, taxonomy search, and retired-family links. The public metadata
  fixture records actual source rows; no production data was changed.
- Authenticated WebKit at 390/1200px passed two-way links, focus, badges,
  no-family-visit recording, pagination, banding search, taxonomic study/country
  expansion, and no page overflow or exceptions. Screenshots reviewed. Local
  initial-page measurements were 556–673 ms; family expansion 71–75 ms.
  PostgreSQL used the family ordering index (174 duck-family rows, 0.068 ms)
  and banding GIN index (OSPR, 0.026 ms); these are local observations.
- Historical record reconciliation remains deferred under `td-b52a90`. No
  species-parent rollup, sighting remapping, or Seen/Need changes were made.
  No commit or deployment performed. The unrelated antimeridian plan is intact.
- Final focused verification: **199 tests across 11 files passed**, including
  eight new taxonomy tests and the country API's explicit taxonomic-sort case.
  Check: zero errors/warnings. Production app/worker build and diff check passed.
- Broader enrichment suite: **67 passed, one scanner test timed out at 30s**.
  The same `scope excludes attempted rows; stale honors windows and AI gating`
  test also timed out at 30s in an isolated worktree at unchanged deployed
  revision `71070d1`. Its scanner implementation is outside this change. The
  temporary baseline worktree was removed after comparison. The focused search
  regression selection independently passed all eight applicable tests.

## Automatic family descriptions (`td-c60291`, 17:37 ET)

The owner identified the missing population process in the original taxonomy
implementation. That manual-only family-notes design is superseded by an
automatic enrichment lane. The matching Markdown and HTML plan now specify it.

- Migration 0058 stores published descriptions, source revision/prose, model
  provenance, retry state and paid-call checkpoints independently of taxonomy
  replacement. A persistent family-only control supports pause and service cooldown.
- The existing worker ensures a durable recurring family job at startup and every
  minute; taxonomy sync nudges it. Each run handles one due family, then returns to
  FIFO order. All 251 currently loaded families are covered by reconciliation.
  Six-month refresh, metadata/membership invalidation, source-gap rechecks after
  30 days, and bounded rapid retries followed by weekly retry require no prose
  entry by the owner.
- Exact scientific-name and family-rank Wikidata matching discovers Wikipedia
  sources. No ambiguous identity is guessed. Descriptions use the configured
  Anthropic Enrichment model and existing usage metering, with exact quotation
  validation and a second source-support audit. Rejected drafts remain unpublished
  and retain corrective feedback for the next attempt. Service errors retain paid
  drafts; insufficient evidence becomes an explicit source gap.
- A short transactional taxonomy lock checks the input hash at publication.
  Changed taxonomy or cancellation prevents publishing an obsolete draft. Failed
  refreshes retain the last good account. No history or Seen/Need reconciliation.
- Taxonomy displays the generated account, source revision, Wikipedia contributor
  credit and CC BY-SA 4.0 license, with explicit AI/not-human-reviewed wording.
  Removed the static one-family notes module. Admin shows coverage, all gaps,
  retries, pause/resume and cooldown. Help and About updated.
- Verification: 221 focused regression tests passed across eight files; 43 queue
  DB tests passed separately. Two queue tests initially encountered an unrelated
  running test-fixture scanner; isolating preexisting jobs resolved both. Optional
  live source and AI tests were exercised separately. Real source retrieval passed
  for Pandionidae, Anatidae and Ardeidae. The live configured Haiku model produced
  an account that the audit initially rejected; a corrected draft passed. This
  verified the rejection/correction path rather than weakening the audit.
- `npm run check`: zero errors/warnings. `npm run build`: web and worker passed.
  Authenticated WebKit at 390/1200 px rendered the real audited account and source
  revision, and exercised Admin pause/resume without overflow or page exceptions.
  Final browser rerun also verified both family controls meet the 48px touch target.
- Evidence: `.local/family-regression.log`, `family-queue-tests.log`,
  `family-live-source.log`, `family-live-ai.log`, `family-live-ai.json`,
  `family-browser.log`, `family-check.log`, `family-build.log`.
- Migration applied only to isolated birds_test. No commit, push, deployment or
  production backfill performed. On release, sync taxonomy to populate the new
  family metadata; the worker then populates descriptions automatically.

## Release authorized

Owner requested deployment of the taxonomy browser, family/banding/taxonomic
study controls, and automatic family enrichment. Preflight production worker
was healthy, idle and unpaused. Release includes migrations 0057 and 0058;
post-deploy verification will enqueue the ordinary Settings taxonomy sync and
confirm the family worker starts. The unrelated antimeridian design remains
outside this release.

## Family description reliability and alternative source (20:18 ET)

- Owner requested fixes for common-family failures and explicitly dropped Birds
  of the World. Added Animal Diversity Web natural-history accounts as the first
  source where an exact bird-family account exists, with Wikipedia as fallback.
  ADW classification-only pages are not prose sources. Parser retains selected
  natural-history paragraphs and excludes navigation, glossary popovers, media,
  conservation assessments and references. Respects the eight-second request
  interval and provider cooldowns. ADW author credit and CC BY-NC-SA 3.0 are
  retained and displayed; Wikipedia summaries retain CC BY-SA 4.0 attribution.
- Family drafting and checking now use their own runtime model setting,
  ai.model.family-enrichment, defaulting to Sonnet 5. Admin AI & Cost offers
  Sonnet 5 and Opus 5; server validation rejects weaker family choices. Species
  enrichment keeps its separate model. Calls remain in the existing cost ledger.
- Replaced brittle copied quotations and minimum quote length with deterministic
  source passage IDs. Validation rejects unknown references with paragraph-level
  diagnostics. The independent audit checks actual claims and their scope; it
  does not reject intentional omissions of taxonomy or species counts. Failures
  now distinguish source retrieval, drafting, auditing and saving stages.
- Prompt version 2 invalidates old checkpoints and schedules refreshes when the
  worker runs the new code. Previously published summaries remain available
  through refresh failures. No schema migration or manual backfill is needed.
  Existing pause, cancellation, retry and publication guards remain in effect.
- Updated Help, About, and both Markdown/HTML taxonomy plans. No BOW integration
  or permission request is planned.
- Verification: 83 focused tests passed across six files (three optional live
  tests skipped in that run). A separate real-provider regression passed: Sonnet
  5 generated and audited Ardeidae and Anhimidae from Wikipedia and Alcedinidae
  from ADW on the first attempt; it rejected a deliberately false universal
  fish-only diet claim. Both draft and audit reported claude-sonnet-5. These are
  sampled checks, not a claim of perfect summaries across every family.
- Authenticated local WebKit at 390/1200 px passed the real audited ADW summary,
  author/source/license links, independent family model options, and model-change
  confirmation/cancel with no overflow or page errors. An initial smoke-script
  failure clicked an already-open Model choice card closed; corrected the script
  to check aria-expanded, then passed both widths. Tests restore isolated DB data.
- npm run check: zero errors/warnings. npm run build: web and worker passed.
  git diff --check passed. Evidence: .local/family-fix-{tests,live,ui,check,build}.log
  and .local/family-fix-live-results.json. No commit or deployment performed;
  production continues using the prior revision until release. Unrelated
  antimeridian planning document remains untouched.

### Family reliability release authorized

Owner approved deployment and the automatic refresh of all families. Preflight:
production revision 546cbdf; database, worker and gallery healthy; family lane
unpaused with no cooldown. Snapshot: 251 families (51 ready, 44 error, 4 without
sources, 152 pending). The family model key is absent, so the new explicit
Sonnet 5 default applies on release. Existing focused tests, real-provider and
WebKit checks, type check and app/worker build apply to this unchanged code.
No new database migration is required. Deploy via scripts/deploy-to-DO.sh and
verify current input hashes, actual served models, publication, and live UI.

## Targeted family source-gap repair (`td-b27120`)

- Completed the remediation implementation without invalidating pipeline-v2
  input/publication hashes. Production remains 240 current descriptions and 11
  gaps; a read-only fingerprint comparison confirmed all 240 were unchanged.
- Added identity-verified scientific-name/redirect resolution and scoped genus
  and species sources. Live discovery resolves all 11 gaps, including two
  Boatbill accounts and three Yellow Flycatcher accounts; Cassowaries/Emu use
  separately scoped accounts and Oilbird uses its living-species account.
- Added complete family natural-history extraction, document-specific passage
  IDs, all-source attribution, retained source/audit diagnostics (additive
  migration 0059), individual gap selection, preservation of current paid drafts,
  and pause checks between source requests. No automatic mass requeue.
- Live Sonnet 5 generation and independent auditing passed for Cassowaries/Emu,
  Oilbird, and Yellow Flycatchers. An intentionally false control was rejected.
  These checks used the isolated test database.
- WebKit passed at 390/1200 pixels: both source revisions linked, individual
  selection available, no overflow or page errors. Focused regression tests,
  type/framework checks (zero warnings), and production build passed.
- Details and release procedure: `docs/2026-09-11-family-source-gap-remediation.md`.
  Production deployment and the selected retry remain for a deployment request.
