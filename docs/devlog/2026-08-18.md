# 2026-08-18 Devlog

## 09:35 EST - No-redaction ruling + alerts link to triggering reports

### Summary
Two Gaylon-driven changes. First, an owner RULING: the private-location
alert redaction (agent-invented during the need-alerts design, never
surfaced to him) is removed — ALL reports show full location detail
everywhere; deployed same conversation (fc96422). Second, td-78a7b1
(P1, filed after his Roseate Tern alert): alerts now link to the actual
eBird report(s) that triggered them — dual-accepted, holding for deploy.

### What Was Done
- Redaction removal: need-alerts-policy display branch deleted, test
  rewritten to PIN full detail for locationPrivate obs, Help corrected.
  Ruling + the process lesson (policy-flavored judgment calls go to
  GAYLON, not just agent review) recorded in memory.
- td-78a7b1 (b0b714b + 2d8dd11 + b8aeb3a): notification click opens the
  closest triggering observation's public eBird checklist (species page
  only as no-subId fallback); AlertCandidate carries ALL triggering
  reports (closest-first, UNCAPPED after CODEX1 flagged my silent 10-cap
  as contradicting the very ruling that spawned the feature); migration
  0021 need_alert_log.reports JSONB written in the atomic settlement
  CTE; /alerts rows un-nested — title links the click URL (new tab only
  when external), each report its own checklist link at the 48px floor;
  service worker opens external URLs directly (cross-origin
  client.navigate unreliable); Help shipped in-commit.
- Review rounds: CODEX1 caught the silent cap + lost 48px targets;
  GROK caught same-tab SPA ejection on external titles + non-links
  styled as tappable (specificity), then walked 390/1280 with seeded
  14-report/fallback/no-subId fixtures. Both ACCEPT.

### Key Technical Insight
A capped list next to an uncapped count ("and 13 more") is a lie the
UI tells quietly — after a no-hidden-data ruling, every bound needs
either removal or visible disclosure. And same-specificity CSS ordering
made a non-link masquerade as a link; win with a more specific selector.

### Next Steps
Gaylon's deploy word for b0b714b..b8aeb3a (migration 0021, no env).

## 13:45 EST - Hotspot Workspace Phase 1: /hotspots/[locId] built and dual-approved

### Summary
Built the Phase-1 hotspot detail page per the GROK-pinned plan
(docs/2026-08-18-hotspot-workspace-plan-CC1.md), then drove it through
three dual-review rounds to CODEX1 APPROVED + GROK ACCEPT on
84a1c4b..17c1cd7 (five commits, local — awaiting Gaylon's deploy word).

### What Was Done
- New gateway src/lib/server/hotspot-page.ts: metadata from the raw
  cached hotspot payloads (bounded @> containment query, measured prod
  scale 48 rows/776 kB), venue chips from the formerly dark
  ebird_locations Google data (chips gated matched+conf≥0.7; Maps pin
  uses the stored place id for ANY matched row), pure-DB Monthly view,
  region-name resolution, and the pure groupRecent shaper.
- /hotspots/[locId]: header (no GPS), stacked ≥48px action bar,
  one-click Load/Refresh via the existing load_hotspots job + jobsPoll
  (failed loads surfaced durably from the queue with role-aware retry
  text), two-tab Recent|Monthly bar, per-state empty states, eBird
  attribution, Help entry in-commit.
- Entry-link swaps exactly per the GROK map (Home BestPlaces,
  PlaceMatches, forecast loaded rows, forecast/species county rows,
  saved trip stops); planner candidates + pick-panel labels untouched.

### Key Technical Insight
Two data-truth blockers caught in review, same class:
1. CODEX1: /data/obs/{locId}/recent returns the LATEST observation per
   species — NOT a checklist feed. Presenting subId groups as
   "checklists with N species" was materially false. Recent is now one
   row per species per day, each row linking to its source checklist.
2. GROK: obsReviewed=false is the NORM on that feed (most valid records
   are never reviewed) and provisionals are excluded by default, so the
   alerts-style unconfirmed predicate chipped 74/74 live rows.
   Unconfirmed on this page = !obsValid only; alerts keep theirs.
Review also enforced 48px floors (month strip 4-col <640px), AAA chip
contrast, and no hidden caps (Monthly "Show all N species").

### Files Modified
src/lib/server/{hotspot-page.ts,+tests,ebird.ts},
src/routes/hotspots/[locId]/{+page.server.ts,+page.svelte},
BestPlaces/PlaceMatches, forecast + forecast/species, trips/[id],
help, plan doc (Recent-contract amendment).

### Next Steps
- Gaylon's word to push + deploy Phase 1.
- Phase 2: forecast declutter (coverage pill + inline manage panel).
- Phase 3: /forecast/data → "Hotspots & data" hub.
- td-24e5e6 (P3): true checklist feed via /product/lists, later.

## 15:00 EST - Phase 1 deployed; Phase 2 (forecast declutter) built and dual-approved

### Summary
Deployed Hotspot Workspace Phase 1 to prod (a6a6b23, health green) on
Gaylon's word, then built Phase 2 — the forecast declutter — through
four dual-review rounds to CODEX1 + GROK ACCEPT on 2171eb7..5db2638
(local, awaiting deploy word).

### What Was Done
- Coverage pill (counts+Manage row, muted years/details line), one-tap
  "Load all remaining (N)" with >30 disclosure copy, inline manage
  panel with name filter (cap suspended while filtering), suggested-
  first ordering consuming the formerly dead view.suggested payload.
  Old bottom <details> block + double-rAF Safari scroll hack deleted.
- Worker budget yield (CODEX1 P1): frequency jobs run in 4-min
  resumable chunks; at each boundary the job re-queues BEHIND anything
  that arrived meanwhile (enqueued_at reset, attempt refunded, cancel
  wins, 'yielded' event via migration 0022). Payload narrows to the
  remainder; base {total, done} keeps whole-batch narration.
- Accounting truth round (CODEX1): ready locs folded on every path;
  failed/skipped chunk-local (never banked — recovered locs must not
  stay "failed"); analyze_counties derives claim-time base from its
  original snapshot (no 400/1000 → 0/600 jumps).
- Coverage-flap round (GROK): first yield pins payload.allCodes so
  jobLocCodes keeps whole-batch covered/queued flags mid-job (no
  Load-all resurrection / double-enqueue); forecast loaded-hotspot
  returnTo now carries the full query (was bare /forecast).

### Key Technical Insight
The yield design needed THREE truth passes: queue fairness (yield),
progress accounting (ready-fold/chunk-local counters), and UI coverage
(allCodes union). Each reviewer caught what the other's angle missed —
the payload is read by three consumers (worker, progress bar, jobs
API) and narrowing it for one broke the others.

### Files Modified
forecast/+page.svelte, job-handlers.ts, jobs.ts, job-policy.ts,
barchart.ts (StopSignal), help, migration 0022, +16 tests (462 total).

### Next Steps
- Gaylon's word to deploy Phase 2.
- Phase 3: /forecast/data → "Hotspots & data" hub (+ GROK notes:
  bottom Close in long panel, persistent #managepanel target).
