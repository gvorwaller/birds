# 2026-08-23

## Field-guide media and enrichment lifecycle

- Confirmed the field-guide sample photo and sound implementation for `td-86a2b6` is present in commit `af05b8a`.
- Completed `td-eae0fc` by extracting the duplicated enrichment chunk lifecycle in `job-handlers.ts` into shared claim, retry/fail, completion, and loop harnesses.
- Preserved drain requeue, cancellation, wall-budget spillover, rate-limit `Retry-After`, `aiOnly` remediation, honest failure, and route-specific dedup behavior.
- Fixed the pre-existing retry drift so exhausted Wikipedia rate-limit jobs retain their chunk summary just like the other terminal paths.
- Scoped field-guide route-contract fixtures to their seeded species code so the tests remain deterministic with the endpoint's intentional `LIMIT 50` and a production-sized test database.

Verification:

- `npm run check` — 0 errors, 0 warnings.
- `npx vitest run src/lib/server/job-handlers.test.ts` — 104 passed.
- `PATH="/Users/gaylonvorwaller/.nvm/versions/node/v24.3.0/bin:$PATH" npm test` — 646 passed, 1 skipped.
- `PATH="/Users/gaylonvorwaller/.nvm/versions/node/v24.3.0/bin:$PATH" npm run build` — application and worker builds passed.
- `git diff --check` — passed.
