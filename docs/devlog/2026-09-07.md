# 2026-09-07 — Home checklist links and release review

## Occurrence-aware nearest reports (`td-fba54e`, follow-up to `td-37c962`)

- The search now prioritizes
  regions with historical reports in the months covered by the recent-report
  window, then other-season positives, unknown coverage, and historical zeroes.
  Distance orders each tier. All candidates remain eligible; history cannot
  establish current absence. Exact region loads only, with twelve sampled
  months and no unmatched names required to classify missing evidence as zero.
- Uses one deadline-bounded query against existing monthly rollups and indexes;
  no migration or cache invalidation is needed. Successful direct eBird answers
  inside the head start avoid the new query. Existing direct-call racing,
  empty-result grace, rate-limit breakers, and probe budgets remain in place.
- Corrected the stop rule to consider the minimum bound of all remaining
  regions. The next occurrence-ranked region may be far away while a closer
  unknown or historical-zero region still needs checking.
- Production read-only verification: 3,110/3,369 subnational1 regions and 91/252
  countries have exact frequency loads. Among the actual 3,443 search candidates,
  Mountain Bluebird's New Mexico region moves from distance rank 282 to rank 8
  from Jacksonville with the current August/September lookback. The first
  occurrence query took 1,915 ms; subsequent EXPLAIN ANALYZE took 164 ms and used
  both existing rollup primary keys. These are query/ordering checks, not an
  end-to-end live eBird or browser test.
- All 37 focused tests pass, including real PostgreSQL SQL tests using isolated
  temporary tables on birds_test, small-budget distant-habitat discovery,
  closer unknown/zero reports, seasonal ordering, calendar boundaries, and
  deadline accounting. Type check: 0 errors/0 warnings. Production build and
  diff check pass. Reviewed the final data flow and updated Help, Version
  History, and the September 1 plan.
- Owner authorized deployment after reviewing the implementation and validation.
  Release uses the standard deployment script, with database/worker health gates;
  the deployed revision and final health result are recorded on `td-fba54e`.

## Direct checklist links (`td-9047db`)

- Reviewed all uncommitted AGY changes independently after CODEX13's earlier
  review and remediation. Home's needed and notable species carry the latest
  report's checklist ID through aggregation and streamed enrichment, including
  per-place expanded rows and the collapsed latest-report links. Missing IDs
  remain absent rather than borrowing another report's checklist.
- The shared MapLink component includes the optional encoded eBird link, even
  without coordinates. Existing Map and Directions callers remain compatible.
  Help and Version History explain the added links and conditional availability.
- Found and fixed a remaining responsive issue: the three-link inline-flex
  group could exceed narrow content columns because it could not wrap. Added
  wrapping and a parent-width constraint while retaining 48px link heights.
- Added actual server-rendered component tests for checklist-only, map-only,
  missing-data, and URL-encoding cases, plus a wrapping contract regression.
  Sixty focused aggregation/enrichment/geo/component/theme/docs tests passed;
  type check has zero errors/warnings; production build and diff check passed.
- Browser skill discovery found no connected browser. Standalone WebKit used
  an actual cached observation with rendered MapLink markup and the actual
  Home/component styles in an isolated layout harness. The 220px container
  regression failed before the fix and passed afterward. Collapsed and expanded
  layouts passed at 320/375/390/640/1024px with no overflow or link intrusion
  into the adjacent column; all links retained 48px height. Screenshot inspected.
  This is component/layout emulation, not a full live Home or physical iPhone test.
- Re-reviewed the final changes: no remaining release blockers found. User
  authorized committing and deploying through the standard health-gated script.
  No schema migration or production fixture changes are needed.
