# 2026-09-09

## Personal species viewing history (`td-bd024a`)

- Added **Viewed species**, linked from Field Guide and species detail pages.
  The collection supports name/code search, recent/alphabetical ordering, and
  first/last viewed dates in UTC, with no hidden result cap. A neutral
  **Viewed in app** badge appears on species pages and Field Guide results.
  Viewing never changes Seen/Need or the life list.
- Record only after a species detail component mounts in a visible document.
  Hover prefetch, loader invalidation, and same-species query changes do not
  create visits. A new displayed navigation updates last viewed while keeping
  first viewed. Failed saves show Retry and do not claim success; retries reuse
  a visit UUID, with database receipts preventing duplicates even after
  intervening visits from another device.
- History belongs to the actual authenticated account, including family
  viewers, independently of the shared life-list owner. Narrow viewer write
  exceptions permit only personal recording, pause/resume, and clear-history
  actions. Requests reject stale account identity and cross-origin recording.
- Added pause/resume and a modal-confirmed clear control. Pausing preserves
  history; clearing preserves the preference. Recording is initially enabled
  and starts with new visits; no historical visits are inferred. Retired
  taxonomy codes remain in the collection with an explicit unavailable label.
- Migration `0052_species_view_history.sql` adds the account preference,
  history, and retry receipts. It was applied only to the isolated test DB.
  The account row serializes recording with pause/clear. History has no
  taxonomy foreign key because taxonomy refresh replaces the taxonomy table.
- Updated Help and Version History. Special interest (`td-e73377`) remains
  separate and unimplemented.
- Validation: 50 tests across nine focused files passed, including real
  PostgreSQL account isolation, repeat/concurrent retry behavior, pause/clear,
  taxonomy retirement, and collection coverage beyond 50 records. Svelte
  check passed with zero errors/warnings; production build and diff check
  passed. WebKit browser checks passed for actual hover preloading (no visit),
  display, query-only navigation, viewer isolation, pause/resume, injected save
  failure with same-ID Retry, result badges, collection search, clear/cancel,
  and owner-settings denial. Layout screenshots inspected at 390/1200px;
  no horizontal overflow or browser exceptions. These are desktop WebKit
  checks with resized viewports, not physical iPhone verification.
- Browser evidence: `.local/verify-species-view-history.mjs`,
  `.local/view-history-browser.log`, and `.local/view-history-{390,1200}.png`.
  The one-off browser harness requires Playwright/WebKit and the isolated
  test server on 5178. Service workers were blocked for reliable request
  failure injection. Do not run the build concurrently with this dev-server
  harness: generated-file changes can trigger reloads and extra visits.
- Owner subsequently authorized deployment. Release checks again passed:
  50 focused tests, zero Svelte errors/warnings, production build, and diff
  check. Deploy through the standard script; final revision, migration outcome,
  health, and authenticated production smoke results are recorded on the td.
