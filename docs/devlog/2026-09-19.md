# 2026-09-19 Devlog

## UX phase 2A release — td-2c866c

Owner approved deployment and explicitly designated the 12 broader-suite
failures non-blocking for this release, recalling previous one-off loading
repairs. This records the owner's release decision, not new verification of
those repairs. No broad suite rerun or unrelated test repair in this release.

Release preflight: 56 focused tests across 11 files passed, npm run check
reported zero errors/warnings, web/worker build passed, and diff whitespace
check passed. Prior independent live test acceptance covered All/Needs save,
JSONB, legacy/viewer/export/shared pages and rejected saves. Migration 0061 adds
nullable count context without a backfill. Preserve the unrelated migration
0049 comment and deploy only the phase 2A code, tests, Help/About and UX docs.

Production release completed through the standard script at `0ddf28a`.
Migration 0061 applied, both PM2 processes reloaded, and public health reported
database/worker/gallery source all ok with the exact revision. Authenticated
All/Needs planner checks passed at desktop/390px with valid signed context,
legacy saved trip 16 retained accurate labels, and HTML export plus verified
hotspot links passed. No browser script errors or trip writes. The unrelated
migration 0049 comment was restored. Evidence is in the phase 2A review record. Next work is
phase 2B's ranking/coverage specification; no later phase implementation is
included in this release.

## UX phase 2B implementation begun — td-f730cf

Wrote an executable specification for explicit, progressive all-hotspot
comparison on Home and the planner. Each location uses its own recent/notable
feed with consistent scope/window, bounded batches/concurrency, visible
coverage and retry/cancellation. Planner selections change only after explicit
Use compared ranking. Version 2 signed snapshots retain per-hotspot provenance
while preserving version 1 and legacy rows. No schema migration or release.

Assigned application work and focused tests to the existing lower-cost built-in
implementer; primary owns independent source and live acceptance. Verified real
Huguenot recent/notable endpoints and 227-species test life list before work.

## UX phase 2B accepted for review — td-f730cf

Implemented explicit per-hotspot comparison on Home/planner, consistent
All/Needs/notable evidence, visible complete/partial/stale coverage, bounded
four-request scheduling, pause/retry/navigation cancellation, and signed v2
saved count context compatible with v1. Route curation changes only on Apply;
manual other locations retain their original tokens. No migration.

Primary review returned selection/token, identity/restart and cancellation
findings for correction. Primary added queued failure/cancellation tests and
fixed zero remaining automatic slots; implementer reviewed those final edits.
Primary rerun: 91 tests/16 files passed; check zero errors/warnings; web/worker
build and diff whitespace check passed. Real test comparison included all 246
reference hotspots in All, Needs and rare mode. Needs reused identical report
cache timestamps (0.355s versus minutes for uncached requests). One transient
provider failure required retry; partly cold timings are labeled honestly.

Chromium verified pause/continue, unchanged route before Apply, repeated Apply,
actual mixed v1/v2 JSONB save, reload, export and legacy compatibility. Test trip
25 retains two compared hotspots plus a manual reported location. Home and
WebKit at 390px comparisons completed 246/246; no overflow or JS errors. Browser
failure injection separately covered restart, partial/stale/no-key and aborted
navigation. Home's open enrichment stream requires waiting for hydrated UI,
not DOMContentLoaded; comparison works while enrichment continues. A first save
navigation run overlapped a build and timed out, then passed in isolation.
Owner 227-species list preserved; test DB/worker/gallery health all ok.

Review/spec/roadmap and linked HTML artifacts updated. No commit/push/deploy;
production remains `0ddf28a` and unrelated migration 0049 comment is preserved.
Phase 2B in review awaiting owner release instruction; parent UX stays in
progress. Next: phase 3 recent-report/Nearest consistency.

## UX phase 2B release authorized

Owner requested deployment and the next phase. Release preflight reran all
91 focused tests across 16 files, framework checks (zero errors/warnings),
and the production web/worker build successfully. Deploy this reviewed scope
through the standard script; preserve the unrelated migration 0049 comment.
No new migration. Production health and authenticated changed-surface checks
will establish release completion before phase 3 implementation begins.

## UX phase 3 implementation begun — td-766bfd

Started td-766bfd plus linked td-3d9544 and td-d71bad from a detailed source-based
specification. Assigned the existing lower-cost implementer: checklist-aware
identity and source/review status, honest missing counts, separate first-seen
records on Home/species, selectable Nearest window/distance, and shared nearby
notable merge without weakening bounded ladder coverage. Primary owns review.

Fresh baseline: owner 227 species; personal September 2 Short-tailed Hawk
checklist still absent from public feeds. Nashville Warbler now appears nearby;
notable's same-place/time pair has DISTINCT checklist IDs, so preserve both.
Equal place/time alone is not proof of duplication. Evidence and limitations
recorded in docs/ux/phase-03-review.md. No phase 3 commit/deploy.

## UX phase 2B production verified — e730c56

Standard deploy pushed main, built remotely, confirmed existing migrations,
reloaded birds and birds-worker. Internal/public health reports exact e730c56
and db/worker/gallery all ok. Authenticated production All/Needs/rare comparisons
completed all five verified small-area hotspots with v2 tokens; planner Apply
and Home completion passed at desktop/390px, without browser errors or trip/
life-list writes. Initial cold timeout attempts passed on repeat without code
changes; large-area latency limitations remain documented. Main/origin matched
and unrelated migration 0049 comment restored. Closed td-f730cf after recorded
review and release; phase 3 work remains uncommitted/undeployed.


## UX phase 3 independently accepted for review — td-766bfd

Implemented checklist-aware public-report identity, conservative review/source
merging, honest missing-count summaries, separate scoped first-seen sightings,
and Nearest window/distance controls with shared nearby notable evidence.
Preserved caller budgets, page-wide limits, settled evidence and navigation
context; fatal refreshes stop new regional waves. Shared personal rows retain
real checklist/map links without changing Seen/Need or public totals.

Primary review added loader and ladder regressions, reproduced/fixed duplicate
truncation, status conflict and settled-wave loss, and received read-only review
of its corrections from the implementer. Final 125 focused tests/15 files pass;
check zero errors/warnings; web/worker build and diff check pass.

Real Chromium desktop/390px acceptance covers Hawk personal data, date/radius,
no-key handling (key restored), distinct Nashville checklists, unit and navigation
context. Family viewer and separate Marcus scope pass. WebKit 390px caught and
verified a native-select sizing fix: controls 48px/16px, search width324px, no
overflow/errors. Hydration-aware retries supersede initial interrupted harness
runs; accepted screenshots inspected. Test list227 and DB/worker/gallery healthy.

Review/spec/roadmap/workflow updated with explicit live-versus-mocked evidence.
Set phase3 and linked td-3d9544/td-d71bad to review, parent remains in progress.
No phase3 commit/push/deploy; production remains e730c56. Unrelated migration0049
comment preserved. Details: docs/ux/phase-03-review.md.

## UX phase 3 release authorized

Owner requested deployment and phase 4. Reran 125 focused tests/15 files,
framework check (zero errors/warnings), production web/worker build and diff
check successfully. Release the reviewed report evidence/personal sighting/
Nearest scope through scripts/deploy-to-DO.sh, preserving the unrelated
migration0049 comment. No new migration. Exact production revision, health
and authenticated Home/species/Nearest acceptance will establish completion.
Phase 4 source investigation is read-only while release is in progress.

### 15:18 EDT — production stream failure traced and corrected

Phase 3 release 334ffff exposed an existing Home delivery defect during real
production acceptance. Concurrent authenticated read-only probes: app port3003
delivered the complete SvelteKit startup script at265ms; nginx port80 withheld
its end and closed at60022ms. App enrichment remained pending at70003ms.
Nginx logs confirm upstream read timeout; the individual45s eBird deadlines
did not bound the multi-wave batch. Disable proxy buffering and bound the whole
Home enrichment window at50s, retaining every base species and successful
detail response, declaring partial coverage, and stopping new calls without
cancelling shared cache work. DB place hydration uses the remaining window.
Focused stalled-provider/DB/late-rejection tests plus Home regressions:31pass.
This is a release repair; phase4 remains separately in progress.
