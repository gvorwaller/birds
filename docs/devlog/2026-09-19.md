# 2026-09-19 Devlog

## UX phase 2A release — td-2c866c

Owner approved deployment and explicitly designated the 12 broader-suite
failures non-blocking for this release, recalling previous one-off loading
repairs. This records the owner's release decision, not new verification of
those repairs. No broad suite rerun or unrelated test repair in this release.

Release preflight: 56 focused tests across 11 files passed, npm run check
reported zero errors/warnings, web/worker build passed, and diff whitespace
check passed. Prior independent live test acceptance covered All/Needs save,
JSONB, legacy/viewer/export/shared pages and rejected saves. Migration 0061 adds
nullable count context without a backfill. Preserve the unrelated migration
0049 comment and deploy only the phase 2A code, tests, Help/About and UX docs.

Production release completed through the standard script at `0ddf28a`.
Migration 0061 applied, both PM2 processes reloaded, and public health reported
database/worker/gallery source all ok with the exact revision. Authenticated
All/Needs planner checks passed at desktop/390px with valid signed context,
legacy saved trip 16 retained accurate labels, and HTML export plus verified
hotspot links passed. No browser script errors or trip writes. The unrelated
migration 0049 comment was restored. Evidence is in the phase 2A review record. Next work is
phase 2B's ranking/coverage specification; no later phase implementation is
included in this release.

## UX phase 2B implementation begun — td-f730cf

Wrote an executable specification for explicit, progressive all-hotspot
comparison on Home and the planner. Each location uses its own recent/notable
feed with consistent scope/window, bounded batches/concurrency, visible
coverage and retry/cancellation. Planner selections change only after explicit
Use compared ranking. Version 2 signed snapshots retain per-hotspot provenance
while preserving version 1 and legacy rows. No schema migration or release.

Assigned application work and focused tests to the existing lower-cost built-in
implementer; primary owns independent source and live acceptance. Verified real
Huguenot recent/notable endpoints and 227-species test life list before work.

## UX phase 2B accepted for review — td-f730cf

Implemented explicit per-hotspot comparison on Home/planner, consistent
All/Needs/notable evidence, visible complete/partial/stale coverage, bounded
four-request scheduling, pause/retry/navigation cancellation, and signed v2
saved count context compatible with v1. Route curation changes only on Apply;
manual other locations retain their original tokens. No migration.

Primary review returned selection/token, identity/restart and cancellation
findings for correction. Primary added queued failure/cancellation tests and
fixed zero remaining automatic slots; implementer reviewed those final edits.
Primary rerun: 91 tests/16 files passed; check zero errors/warnings; web/worker
build and diff whitespace check passed. Real test comparison included all 246
reference hotspots in All, Needs and rare mode. Needs reused identical report
cache timestamps (0.355s versus minutes for uncached requests). One transient
provider failure required retry; partly cold timings are labeled honestly.

Chromium verified pause/continue, unchanged route before Apply, repeated Apply,
actual mixed v1/v2 JSONB save, reload, export and legacy compatibility. Test trip
25 retains two compared hotspots plus a manual reported location. Home and
WebKit at 390px comparisons completed 246/246; no overflow or JS errors. Browser
failure injection separately covered restart, partial/stale/no-key and aborted
navigation. Home's open enrichment stream requires waiting for hydrated UI,
not DOMContentLoaded; comparison works while enrichment continues. A first save
navigation run overlapped a build and timed out, then passed in isolation.
Owner 227-species list preserved; test DB/worker/gallery health all ok.

Review/spec/roadmap and linked HTML artifacts updated. No commit/push/deploy;
production remains `0ddf28a` and unrelated migration 0049 comment is preserved.
Phase 2B in review awaiting owner release instruction; parent UX stays in
progress. Next: phase 3 recent-report/Nearest consistency.

## UX phase 2B release authorized

Owner requested deployment and the next phase. Release preflight reran all
91 focused tests across 16 files, framework checks (zero errors/warnings),
and the production web/worker build successfully. Deploy this reviewed scope
through the standard script; preserve the unrelated migration 0049 comment.
No new migration. Production health and authenticated changed-surface checks
will establish release completion before phase 3 implementation begins.

## UX phase 3 implementation begun — td-766bfd

Started td-766bfd plus linked td-3d9544 and td-d71bad from a detailed source-based
specification. Assigned the existing lower-cost implementer: checklist-aware
identity and source/review status, honest missing counts, separate first-seen
records on Home/species, selectable Nearest window/distance, and shared nearby
notable merge without weakening bounded ladder coverage. Primary owns review.

Fresh baseline: owner 227 species; personal September 2 Short-tailed Hawk
checklist still absent from public feeds. Nashville Warbler now appears nearby;
notable's same-place/time pair has DISTINCT checklist IDs, so preserve both.
Equal place/time alone is not proof of duplication. Evidence and limitations
recorded in docs/ux/phase-03-review.md. No phase 3 commit/deploy.

## UX phase 2B production verified — e730c56

Standard deploy pushed main, built remotely, confirmed existing migrations,
reloaded birds and birds-worker. Internal/public health reports exact e730c56
and db/worker/gallery all ok. Authenticated production All/Needs/rare comparisons
completed all five verified small-area hotspots with v2 tokens; planner Apply
and Home completion passed at desktop/390px, without browser errors or trip/
life-list writes. Initial cold timeout attempts passed on repeat without code
changes; large-area latency limitations remain documented. Main/origin matched
and unrelated migration 0049 comment restored. Closed td-f730cf after recorded
review and release; phase 3 work remains uncommitted/undeployed.


## UX phase 3 independently accepted for review — td-766bfd

Implemented checklist-aware public-report identity, conservative review/source
merging, honest missing-count summaries, separate scoped first-seen sightings,
and Nearest window/distance controls with shared nearby notable evidence.
Preserved caller budgets, page-wide limits, settled evidence and navigation
context; fatal refreshes stop new regional waves. Shared personal rows retain
real checklist/map links without changing Seen/Need or public totals.

Primary review added loader and ladder regressions, reproduced/fixed duplicate
truncation, status conflict and settled-wave loss, and received read-only review
of its corrections from the implementer. Final 125 focused tests/15 files pass;
check zero errors/warnings; web/worker build and diff check pass.

Real Chromium desktop/390px acceptance covers Hawk personal data, date/radius,
no-key handling (key restored), distinct Nashville checklists, unit and navigation
context. Family viewer and separate Marcus scope pass. WebKit 390px caught and
verified a native-select sizing fix: controls 48px/16px, search width324px, no
overflow/errors. Hydration-aware retries supersede initial interrupted harness
runs; accepted screenshots inspected. Test list227 and DB/worker/gallery healthy.

Review/spec/roadmap/workflow updated with explicit live-versus-mocked evidence.
Set phase 3 and linked td-3d9544/td-d71bad to review, parent remains in progress.
No phase 3 commit/push/deploy; production remains e730c56. Unrelated migration 0049
comment preserved. Details: docs/ux/phase-03-review.md.

## UX phase 3 release authorized

Owner requested deployment and phase 4. Reran 125 focused tests/15 files,
framework check (zero errors/warnings), production web/worker build and diff
check successfully. Release the reviewed report evidence/personal sighting/
Nearest scope through scripts/deploy-to-DO.sh, preserving the unrelated
migration 0049 comment. No new migration. Exact production revision, health
and authenticated Home/species/Nearest acceptance will establish completion.
Phase 4 source investigation is read-only while release is in progress.

### 15:18 EDT — production stream failure traced and corrected

Phase 3 release 334ffff exposed an existing Home delivery defect during real
production acceptance. Concurrent authenticated read-only probes: app port 3003
delivered the complete SvelteKit startup script at 265 ms; nginx port 80 withheld
its end and closed at 60,022 ms. App enrichment remained pending at 70,003 ms.
Nginx logs confirm upstream read timeout; the individual 45-second eBird deadlines
did not bound the multi-wave batch. Disable proxy buffering and bound the whole
Home enrichment window at 50 seconds, retaining every base species and successful
detail response, declaring partial coverage, and stopping new calls without
cancelling shared cache work. DB place hydration uses the remaining window.
Focused stalled-provider/DB/late-rejection tests plus Home regressions: 31 pass.
This is a release repair; phase 4 remains separately in progress.

### 15:26 EDT — release 505382b verified; phase 4 correction pass

Standard deployment complete, nginx syntax/reload passed, PM2 app+worker and
DB/worker/gallery health all good at 505382b. Authenticated public smoke passed:
Home interactive 599 ms after shell/full response 9,919 ms; personal Hawk checklist
on Home/species; two distinct Nashville reports; context-preserving links;
invalid-window 400;390px controls at least 48px high without overflow; no JS errors. Seen snapshot 227.
Phase 4 td-c9e804 specified/started using lower-cost implementer. Primary rejected
incomplete first handoff (polling/state/scoping/cache/auth/UI contract gaps) and
returned concrete corrections before acceptance. Phase 4 remains undeployed.


### 16:17 EDT — UX phase 4 independently accepted for review

Lower-cost implementer completed the specified metadata recovery and job-state
work after primary correction passes. Official hotspot identity is validated
and cached on demand; personal names cannot verify IDs. Shared presentation and
polling preserve paused/retry/scheduled identity, isolate unrelated Forecast
work and refresh relevant hotspot data after completion. Help/About updated.

Primary ran 131 focused tests in 13 files, zero-error/warning framework check,
web/worker build and diff check. Final display polish removed duplicate status
explanations, restored retry attempt counts, linked directly to Background work
and replaced a stale queued receipt; check/build and affected browser checks
passed again. No new schema or worker scheduling changes.

Real test: removed selected Celery official cache evidence reversibly; live
verification recovered exact identity/county and queued job 5425. Actual global
pause/resume was reflected correctly. Its history download timed out at eBird
and entered normal retry, truthfully displayed; do not claim that download passed.
Separate Lake Uihlein refresh 5427 succeeded with 103 species and automatic
hotspot data refresh preserving tab/month/window/return context. Real Forecast
kept unrelated work out of its local progress. Owner seen count remains 227,
family enrichment stays paused, global worker resumed and test health all ok.

Chromium/WebKit injected state display checks are labeled separately from real
worker evidence. Both browsers also passed viewer controls/403 and actual native
JavaScript-disabled personal-ID submission: 422 plus exact unresolved alert,
query context retained, no job. Final WebKit phone layouts have no overflow or
page errors; refresh control 49px. Temporary fixture cleanup was audited and
recorded transparently. See docs/ux/phase-04-review.md and refreshed HTML report.

Submit td-c9e804 for owner review; parent remains in progress. Production remains
healthy at 505382b (main/origin agree), phase 4 remains uncommitted/undeployed,
and unrelated migration 0049 comment is preserved. Phase 5 will cover shared
navigation context after owner review and a separate release instruction.

### 16:40 EDT — phase 4 release authorized

Owner requested phase 4 deployment and phase 5 implementation. Reran the 131
focused tests, framework check (zero errors/warnings), production web/worker
build and diff check. Release the reviewed phase 4 code and documentation via
the standard script, preserving the unrelated migration 0049 comment. No new
migration. Production acceptance will check exact revision, job-state API/hub,
hotspot context and controls without initiating production loads or list writes.
Phase 5 investigation is read-only until release verification completes.


### 16:43 EDT — phase 4 production verified; phase 5A started

Standard deployment of 05ba3af completed with PM2 app/worker and DB/worker/gallery
healthy. Authenticated production API/hub, history disclosure, hotspot context,
verification controls and 390px checks passed without JS errors or production
load/list/trip writes. Main/origin agree; unrelated migration 0049 comment restored.
Closed td-c9e804 after owner release authorization and independent acceptance.

Phase 5 split explicitly into 5A shared core + trip/hotspot/species + place safety,
then 5B wider route adoption. Detailed spec docs/ux/phase-05a-navigation-context.md;
started td-ea384e. Use supported SvelteKit page.state and account/tab sessionStorage,
ordinary safe links and nonrecursive immediate fallbacks; preserve all content
queries, origin rows/expansion and explicit location choice instead of Home.
No phase 5 commit/deployment authorized in this turn.


### 17:25 EDT — phase 5A independently accepted for review

Implemented account/tab-scoped journey context, named safe ordinary fallbacks,
trip/hotspot/species origins and expanded-list restoration, plus explicit
Forecast location choice when coordinates are missing. Nearby-report names
now enter Birds hotspots while retaining separate external eBird links.
Primary review corrected router readiness, reload recovery, account isolation,
ancestor link/state handling, streamed origin restoration and native Back scroll.

83 focused tests, zero-error/warning framework check, web/worker builds and
diff check passed. Real Chromium/WebKit phone journeys, desktop return flow,
refresh/Back/Forward, month/window preservation, family-account switch, native
and blocked-storage fallbacks, explicit real-place Forecast and a second bird
report journey passed. Myakka fingerprint unchanged (5657 rows), owner227,
family paused. Detailed evidence: docs/ux/phase-05a-review.md.

Submit td-ea384e to review; parent td-8ff597 remains in progress. No phase5
commit/deploy; production stays05ba3af. Phase5B wider adoption is next after
owner review/release authorization. Preserve unrelated migration0049 comment.

### 18:05 EDT — phase 5A release authorized

Owner requested deployment of phase 5A and implementation of phase 5B. Release
preflight reran 83 focused navigation/location tests, framework checks with zero
errors and warnings, the production web/worker build, and the whitespace check.
Release the reviewed journey-context scope through scripts/deploy-to-DO.sh,
preserving the unrelated migration 0049 comment. There is no phase 5A migration.
Exact production revision, health, and authenticated changed-surface browser
checks will establish completion before phase 5B implementation starts.

### 18:07 EDT — phase 5A production verified; phase 5B specified

Standard deployment released `c731094`; all 60 migrations were already applied,
PM2 reloaded Birds and its worker, and public health reports the exact revision
with DB, worker and gallery healthy. Authenticated Chromium at 390px completed
Trip 9 → Myakka monthly September → all 110 species → Common Grackle → Myakka →
Trip 9. Both source rows regained focus, the month and expanded list survived,
there was no overflow or page error, owner Seen remained 227, and the smoke made
no production mutations. Main and origin agree; migration 0049's unrelated
comment was restored. Approved td-ea384e after production acceptance.

Wrote the phase 5B executable specification for remaining Field Guide, collection,
Home/Nearest/Alerts and Forecast/county entry points. It extends the released
shared context without changing data scope, rankings, geographic selection,
drawer behavior or species-page section design. Terra Medium owns implementation;
primary owns review, realistic browser acceptance and td state.

### 18:47 EDT — phase 5B independently accepted for review

Terra implemented remaining journey-context adoption; primary source review
returned label, source registration, Taxonomy, Home/report, Life-list and
Forecast gaps for correction, then completed an independent common-core pass.
Field Guide, Taxonomy, Viewed, Special interest, Photos, Life, Home, Nearest,
Alerts, bird Forecast and Hotspots & data now retain named ancestry, complete
content URLs and stable source rows without changing result scope or mutations.

Real Chromium/WebKit 390px journeys passed Field Guide page 2 → Black Rail →
Rallidae → Ridgway's Rail and exact returns; searched Jacksonville Home; Common
Grackle → Florida → Manatee → real hotspot and returns; White-rumped Sandpiper
Nearest → Big Talbot report and return; Photos and Alerts. Chromium 1280px also
passed. Reload, `#results`, native/JavaScript-off, blocked storage and family
Life-list namespace checks passed in both browsers with no page errors or
overflow. Review fixed fragment loss and a route-render/page-state race in the
shared navigation adapter, with regressions.

Test job 5433 used the normal app/worker path to load six selected Manatee
hotspots because the copied cache had none of its bounded selection loaded; it
succeeded and enabled real Forecast hotspot proof. Protected Myakka remains
5,657 rows with its original fingerprint, owner Seen 227 and family enrichment
paused. Test health is DB/worker/gallery healthy. Final gate: 89 focused tests,
framework check 0 errors/0 warnings, web+worker build and diff check passed.
Detailed evidence: docs/ux/phase-05b-review.md. Submit td-750277 for review;
parent td-8ff597 remains in progress. Phase 5B is uncommitted and undeployed;
production remains healthy at c731094. Preserve unrelated migration 0049.

### 18:53 EDT — phase 5B release authorized

Owner requested phase 5B deployment and phase 6 implementation. Release
preflight reran 89 focused tests, framework checks with zero errors/warnings,
the production web/worker build and whitespace check. Release only the reviewed
phase 5B scope through the standard deployment script; preserve the unrelated
migration 0049 comment. No new dependency or migration. Verify the exact public
revision, health and authenticated changed-surface journeys before closing
td-750277 or starting phase 6 implementation.

### 19:02 EDT — phase 5B production verified; phase 6 specified

Standard deployment released `9abbe8a`; main and origin agree, all 60 migrations
were already applied, both PM2 processes are online, and public/internal health
report the exact revision with database, worker and gallery healthy.
Authenticated Chromium at 390px repeated Field Guide page 2 → Black Rail →
Rallidae → Ridgway's Rail and exact returns, plus Photos and Alerts returns.
Filters, the unknown `future=kept` parameter and `#results` survived; no page
errors, overflow, session creation or data mutation occurred. Owner Seen stayed
227. The unrelated migration 0049 comment was restored byte-for-byte. Approved
and closed td-750277 after production acceptance.

Phase 6 implements F03/F08/F09 within a frozen interaction boundary: a complete
keyboard-modal drawer for both actual openers, Forecast map-editor reveal and
cancel focus, and 48px/16px sizing on those touched shell/picker controls. It
does not absorb phase 7 Field Guide/species navigation or broad trip-editor
work. Detailed contract: docs/ux/phase-06-shell-map-controls.md. Terra Medium
implements; primary independently reviews and exercises Chromium/WebKit before
submitting the child td. No phase 6 commit or deployment is authorized yet.

### 19:08 EDT — phase 6 independently accepted for review

Implemented a keyboard-complete Navigation drawer for both actual openers,
Forecast map-editor heading/reveal/cancel behavior and 48px/16px sizing on the
touched shell and shared picker controls. Help and About describe the visible
behavior. The implementation changes no route data, calculation, persistence,
permission, dependency or schema behavior.

Primary browser review returned two defects to Terra: scroll clearance was on
the editor section instead of the focused heading, placing it behind the fixed
nav; WebKit also exposed an intermittent dismissal race where a still-attached
focus guard could steal focus from the restored opener. Both were corrected
with source-contract regressions.

Final Chromium/WebKit checks passed drawer entry, two-way containment, Escape,
scrim, exact phone/desktop opener restoration, route selection, dark/light
themes, Forecast focus/reveal/cancel and a real Myakka geocode/apply path.
Measured targets are 48–49px and WebKit map-search text is 16px. Twenty-five
focused tests, framework check 0/0, production web/worker build and diff check
passed. Protected Myakka remains 5,657 rows with the same fingerprint, owner
Seen 227 and family paused. Physical iPhone verification remains explicit and
outstanding. Submit td-f3ccda for review; parent td-8ff597 remains in progress.
No phase 6 commit/deployment authorized. Preserve the unrelated migration 0049
comment.
