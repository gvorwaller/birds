## Post-7B UX continuation plan — td-8ff597

Reconciled the live backlog after every Phase 1–7B implementation ticket and
linked Phase 3 repair closed. Wrote `docs/ux/future-implementation-plan.md` as
the next implementation sequence without starting code or creating duplicate
child tickets. Owner-reported production regressions are Gate 0 and take
precedence over dependent work.

The recommended sequence is: trustworthy test/API/geographic prerequisites;
county/hotspot/map-radius discovery; explicit All/Need/Seen and
recent-versus-historical scope; place/scope handoff into Forecast, Home and
trips; viewer setup clarity; remembered species disclosures; field-trip stop
check-off and separately justified route/weather improvements; then enrichment
approval, operations visibility and measured performance. Existing source
tickets remain authoritative and retain their statuses. Gated taxonomy,
licensed abundance and media-upload ideas remain explicit deferrals.

The first future implementation work order should be Phase 8A Field Guide
location choices, but only after the owner's regression list is reproduced and
blocking regressions are stabilized. This turn changes documentation and td
planning records only; no application code, data, commit or deployment.

## Phase 8A Field Guide geographic selection — td-82fbc1

Implemented the frozen Phase 8A contract with CC1 as the sole implementation
writer, Codex primary review and independent GROK QA. Field Guide historical
scope now supports country, state/region, loaded county-equivalent, one exact
verified loaded hotspot, and an explicit map point plus 1–200 whole-mile
radius. Canonical URLs, strict ancestry/identity validation, truthful coverage,
pre-pagination intersections, unknown-parameter preservation, native no-JS
hierarchy changes and map Apply/Cancel behavior are covered by focused tests.

Primary review returned the no-JS stale-descendant path, malformed intent
hardening and inaccurate About wording for correction. GROK found no P0–P2 and
one P3: duplicate accessible button names while the map chooser was open. The
shared picker now uses **Search place**; input/grid shrink rules also remove the
newly exposed 320px overflow without changing wider layouts or other picker
callers.

Real-data acceptance matched Sarasota County at 376 species, Myakka River SP
at 244, and the recorded-coordinate 25-mile circle at 99 loaded hotspots and
351 species. Chromium/WebKit, owner/viewer, mobile/desktop, JavaScript/no-JS,
return navigation, malformed URLs and antimeridian coverage passed. Final
focused runs, `npm run check`, `npm run build` and `git diff --check` are green;
owner test data remains 227 seen species with no fixtures or throwaway users.
Review: `docs/ux/phase-08a-review.md`. Nothing was committed, pushed or
deployed.

## Phase 8B Hotspots & data geographic discovery — td-687b1c

Implemented the frozen Phase 8B contract with CC1 as sole implementation
writer, Codex primary review and two-pass independent GROK QA. Hotspots & data
now finds reference countries/regions, loaded counties/equivalents and verified
eBird hotspots by typed query or explicit map point plus 1–200-mile radius.
Exact totals and complete 50-row pagination replace the silent cap. Failed
locations without affirmative hotspot evidence remain visibly unverified and
open the existing failed-load recovery row.

Primary review caught an implicit eBird-call path for keyed owners. Discovery
hotspot links now open the local Monthly view, and typed/map/selection landings
skip server fan-out and automatic hotspot-count requests. Keyed guards, SQL
capture and browser traps prove selection is read-only and local until the
owner takes an explicit action. GROK's initial P2s on unverified targeting and
inventory hotspot links were fixed; its narrow retest closed every P0–P3.

The current test snapshot reconciles to 2,135 coordinate-known verified
hotspots and 349 within 25 miles of Myakka River SP, over seven complete pages,
with four loaded counties and Florida represented. Final evidence includes 32
files/336 focused tests from CC1, 10 files/100 tests from primary review and the
same 100-test narrow GROK rerun, plus clean check/build/diff gates and two-engine
owner/viewer/no-JS/mobile browser matrices. Review:
`docs/ux/phase-08b-review.md`. Nothing was committed, pushed or deployed.
