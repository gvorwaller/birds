# 2026-09-18 Devlog

## td-4b5248 — Correct the monthly-rollup migration estimate

Replaced the incorrect row-count estimate in migration 0049's header with the
production measurement recorded in td-4b5248: on 2026-09-03,
`species_month_freq` held 9,546,716 rows (1016 MB), and `loc_month_samples`
held 109,860 rows. The comment identifies the unchecked estimate's provenance
as commit `8b642aa` (2026-08-31) and explicitly labels the measurements as
historical, not current counts. No new production measurement was performed.

Validation: compared migration contents against HEAD after removing SQL comments
and blank lines; executable SQL is identical. `git diff --check` passes.
No schema, runtime behavior, or production data changed; the migration was not
run. Help/About updates and application tests/build are unnecessary for this
documentation-only correction. Changes remain uncommitted and undeployed.

Submitted td-4b5248 and its queue epic td-81a92d for review at the owner's request.
The other five epic children are already closed; the final comment correction
is ready for review.

## Integrated UX backlog roadmap — td-8ff597

Reviewed all 31 open tickets and targeted current navigation, context-link,
taxonomy, location-control, Nearest, and Admin source. Added
`docs/2026-09-18-birds-ux-roadmap.md`, with a complete ticket disposition table,
shared interaction/visual contracts, ordered implementation packages, and
acceptance journeys. Recorded the owner's clarification on td-d2bb08: add
county, hotspot, and map/radius choices to Field Guide location selection.

The proposed order starts with preserving deep navigation/search context,
then unifies location selection and all/need/seen discovery, then improves
species disclosures and trips. Reliability, test isolation, performance,
enrichment approval, and admin work have a supporting lane. Existing shipped
behavior and stale parent-ticket status are distinguished from new work;
explicitly deferred features remain deferred. The independent app-wide UI/UX
audit is still a future step, not completed by this source/backlog review.

Validation: automated comparison confirms all 31 open snapshot IDs occur
exactly once in the disposition table. Documentation-only; no runtime tests,
browser audit, production queries, commit, or deployment. Logged the plan in
td-8ff597 and the clarification in td-d2bb08 without changing ticket status,
priority, or parent. Preserved the preceding uncommitted migration comment.

## Broad UX review and first-release specification — td-8ff597

Owner authorized the independent UX investigation and unrestricted changes to
test lists/data for it. Started td-8ff597 and verified the existing Vite test
server plus isolated birds_test database on 15436. Reviewed 20 principal routes
at desktop and phone widths, then exercised filtered Field Guide/taxonomy,
trip/hotspot/species return paths, county drill-down, Photos, populated life
lists, ordinary no-key and read-only accounts, and test mutations. Used WebKit
for the initial pass and Chromium for focused work; WebKit stalled on Settings,
so the settings/admin/reference pass used Chromium. Physical iPhone/PWA,
live recent-eBird happy paths and worker completion were unverified in that initial pass; the corrective follow-up below supersedes that limitation.

Reproduced lost trip ancestry, county links dropping return context, Photos
returning to Home, and a missing-coordinate hotspot's “here” forecast silently
opening saved Home. Verified the drawer does not move/contain focus or close
with Escape. Measured deep results/sections and small controls; confirmed that
taxonomy focus, direct guide filters, and explicit full monthly expansion work.

Added docs/2026-09-18-birds-ux-review.md and
docs/2026-09-18-birds-ux-release-1.md; refined the existing roadmap without
creating a competing backlog. Produced linked HTML/Markdown deliverables and
an interactive first-release concept in the Codex task outputs directory.
Browser-verified the concept's filter disclosure, bird/return navigation,
section focus, location recovery/cancel and narrow layout. These are proposed
UI changes, not implemented application functionality.

Test mutations: created trip 16 (“UX audit — September 18”) and saved/reloaded
its explanatory notes; saved then removed Marbled Godwit from Special interest;
normal viewing history writes. Temporarily removed the ordinary account's
test API key and restored it after no-key browsing checks. The snapshot already
had a one-fixture-bird owner life list, missing recent caches, invalid live API
access and no worker; these were explicitly separated from product findings.

No runtime code, schema, production data, commit or deployment changed. The
prior migration-comment edit remains intact. Application tests/build were not
run for documentation/artifact work; future release gates are in the spec.


## Corrective live-data UX investigation — td-8ff597

Owner explicitly authorized using production eBird credentials and copying
production data if needed. Read production only to transfer the owner's API
and login credentials in memory, re-encrypted them under the test secret, and
restored the real 227-species life list plus lifer coordinates into birds_test.
No plaintext credential files/logs; no production writes. The existing ~37.9M
frequency-row corpus was sufficient, so no full snapshot replacement was needed.

Built the existing worker bundle and started a dedicated detached test worker
using .local/start-test-worker.mjs, guarded to 127.0.0.1:15436/birds_test.
Health reports db/worker/gallery_source ok. Test push delivery is disabled,
family paid enrichment paused, and unrelated daily enrichment deferred; normal
eBird work runs. Local launch/log files are ignored. No runtime source changes.

Re-ran Home (54 needs, 35 places), populated Forecast (127/130 loaded),
hotspot recent data, Nearest, both automatic planner scopes, and saved/reloaded
trip 18 with three stops, map/driving route, weather and tides. Huguenot refresh
job 5192 succeeded in 37.1s. Real login/life-list sync job 5193 succeeded in
1.3s, matching 227 of 229 rows with two hybrid/slash rows explicitly unmatched.
First-load job 5195 succeeded for previously unloaded Lake Uihlein. Celery
Fields' metadata mismatch was recovered via real nearby hotspot discovery;
its job 5194 entered delayed retry after an upstream 30s timeout, with visible
retry status. This is a tested error/retry flow, not an untested-worker claim.

Rewrote the review, roadmap and first-release specification. Added F12–F15:
private/other observation locations misrepresented as hotspots; All-mode labels,
partial rankings and incompatible saved/current counts; source/window/review
status and duplicate-report semantics; load prerequisite/status recovery.
Reproduced td-3d9544 against current feeds: the owner sighting exists in the
synced list but is absent upstream in public recent data, while other private
locations remain present. Logged evidence without claiming the upstream reason.

Updated HTML/Markdown deliverables and live screenshots. No commit, deployment
or application implementation. Existing migration comment change preserved.
Physical-device, push delivery and paid AI generation remain outside these
executed tests; earlier invalid-key/no-worker limitations are resolved.


## UX phase 1: specification / implementer / independent review — td-d22017

Owner approved a lower-cost built-in implementer with primary-agent specs and
review, phase by phase. Added docs/ux/implementation-workflow.md and detailed
phase-01-trip-location-identity.md. Delegated runtime/test/Help/About work to
gpt-5.6-luna; primary retained specification decisions, diff review and live
acceptance. No production work or commits authorized/performed.

Phase 1 now auto-selects only cache/reference-verified eBird hotspots, preserves
all reported candidates for explicit manual selection, and correctly labels
unverified saved locations, including legacy trip 18 and viewer pages. One
hotspot-reference outcome supplies identity plus metadata, without a duplicate
request after failure. Added batched saved-id verification and stale/failure
semantics. No schema changes or destructive backfill.

Review required removal of a duplicate reference-fetch path, stronger curation
reset identity, accessible action names/48px targets, a historical/shortage
regression and a phone layout correction. Final primary browser checks retained
36 candidates, selected Huguenot/Spoonbill/Omni automatically, manually added
and saved the yard as trip 19, verified data after reload, checked old trip 18
and family viewer labels, and changed 3 to 4 stops without retaining the yard.
Final phone card text/action layout visually inspected.

Primary independently reran 14 focused Vitest tests successfully. Implementer
final npm run check: zero errors/warnings; npm run build: web and worker passed.
Diff whitespace check passed; test db/worker/gallery health ok. Review record
and linked HTML outputs document evidence and phase boundaries. Phase 2 will
address count meaning/ranking; no claim that other UX findings are fixed.

## UX phase 1 release preparation

Owner requested commit/deploy and continuation to the next phase. Repeated the
release gates: 14 focused tests passed, framework check had zero errors/warnings,
web/worker build and diff whitespace checks passed. Release scope includes the
UX roadmap/audit/spec/review documents and phase 1 runtime/tests/Help/About.
The unrelated migration-0049 comment correction is excluded and preserved.
Production verification will be recorded after the scripted deployment.

## UX phase 1 production release and phase 2A handoff

Owner authorized commit/deploy and next phase. Released 582f665 through the
standard deployment script; origin/main matches. No pending migrations. Both
PM2 Birds processes reloaded online; public db/worker/gallery_source health ok
and version 582f665. Authenticated production browser smoke checked 36 planner
candidates, verified all three automatic stops against real reference caches,
checked unverified labeling/390px layout, and opened existing trip 16 with seven
stops without script errors. No trip/list edits in production. Restored the
unrelated migration 0049 comment. Closed td-d22017 after prior independent review.

Split phase 2 into 2A count contracts/persistence and 2B comparable rankings. Wrote
detailed phase-02a spec and started td-2c866c with the same lower-cost implementer.
Primary retains independent review. No phase 2 commit or deployment authorized
as part of the already-reviewed phase 1 release; phase 2 will end at review.

## UX phase 2A implementation and independent review — td-2c866c

Lower-cost implementer delivered mode-correct labels, signed planner context
(account/scope/expiry/identity binding), migration 0061 nullable JSONB without a
backfill, atomic save validation, legacy unknown-context rendering, independent
current-nearby counts, and export/share consistency. Primary review required
exact upstream rounding, timestamp validation/timezone, fail-closed external
hotspot links, valid-token mutation tests and the actual transaction helper
in rollback tests. Help and About updated. No remaining blocker in 2A scope.

Primary independently ran 56 focused tests in 11 files successfully; final
framework check zero errors/warnings; implementer web/worker build passed
after runtime corrections. Primary reviewed diff and directly exercised test
All trip 22 / Needs trip 23 (four stops each, including manually added yard),
JSONB persistence, reload, legacy trip 18, family viewer, export, missing API key
(restored finally), and 10 invalid/cross-account/viewer action cases without
creating trips. Chromium desktop/390px and WebKit 390px passed; phone visuals
inspected. Public share opened without cookies with correct verified/unverified
links, then revoked and 404 confirmed. Test worker/database/gallery healthy.

The implementer's broad suite ran 1,552 passing, 5 skipped, 12 failing across 5
DB-backed files, and cleared the shared test owner's seen list. Restored 227
real rows from read-only production before rerunning acceptance; API key and
seen count rechecked afterward. No broad-suite pass or unverified claim that
all failures predate this work. Evidence logged on td-b29d1c/td-c41126 and in the
phase review. No more broad tests ran.

Migration 0061 is applied in test only. Phase 2A is in review with primary
independent approval recorded, uncommitted/undeployed. Phase 2B comparative
rankings remain outstanding; investigation notes record 246 nearby reference
locations versus 195 base-feed species and 73 Huguenot location-feed species.
The unrelated migration 0049 comment is preserved. Production remains 582f665.
